# Phase: Exchange Wars — Phase 18b: Adversarial review (17w–18a) + lock the gap-to-#1 gate at #2 (Brick 262)

**Started:** 2026-06-13
**Hat:** Reviewer → Builder (quality gate: review 5 bricks, close the one finding)
**Goal:** Run the periodic adversarial review over 17w–18a (5 bricks since the last review at 17v) and close
the one MEDIUM it surfaced: the 18a gap-to-#1 "redundant at #2" premise is unit-asserted but never
verified at the COMPONENT gate, so a future loosening of `meRank >= 3` to `>= 2` would pass every test
while a #2 player sees two identical lines (🎯 rank-above == 👑 gap-to-#1). Add a render test that pins
the 👑 line SUPPRESSED at #2 (while 🎯 still shows).
**Done condition:** A render test asserts `.gaptop` is null at meRank 2 and `.rankgap` is present; suite + e2e green.

## Why this brick
The review cadence is ~every 5 bricks (17i: 16w–17h; 17p: 17i–17o; 17v: 17q–17u); 17w/17x/17y/17z/18a are
5 unreviewed bricks. Verdict: **SOUND** — 17w copies-before-sort + guards empty drops; 17x ordering claim
holds (REGIONS is depth-ordered, filter preserves it, `||` short-circuits the elite/pool double-count); 17y
`qty−have` only renders on `!ready` (always positive) + the no-onSelect fallback is correct; 17z's refactor
preserves the null-sink (the sort + cell both null exactly when `ema ≤ 0`, and every `view.markets` row
carries `ema`). The one MEDIUM is a TEST gap, not a code defect — the gate (`< 3 → null`) is already
correct; it just isn't guarded against regression. Closing it makes the "two distinct climbs" invariant
enforceable. (LOW noted, not changed: 17z's "Noema Co" component assertion is somewhat redundant with the
`momentum()` unit test — but it's valid (rendered name confirmed) and still guards the divide-by-zero→Infinity
case, so it stays.)

## Design — one render test
- `app.test.tsx`: render `LeaderboardPanel` with the handle matching the **#2** row of a 3-row board →
  assert `container.querySelector('.gaptop')` is null (the 👑 summit line is suppressed where it would
  duplicate the rank-above gap) AND `.rankgap` is present (the next-rung climb still shows). This locks
  the gate at #3 — loosening to `>= 2` now fails a test.

## Scope (in)
- `packages/ui/test/app.test.tsx`: the #2-suppression render test

## Scope (out)
- No source change (the gate is already correct); LOW left as-is (valid + guards Infinity); no engine change → no redeploy

## Subsystems touched
- packages/ui/test/app.test.tsx

## Gates
- [x] render test: `.gaptop` null at #2, `.rankgap` present (4,000) at #2 — locks the gate at meRank>=3
- [x] UI suite (429, +1) + e2e (10) green; typecheck clean
- [x] test-only — no source/bundle change (the gate was already correct); no engine change, no redeploy

## Open questions
- None — the review is done (SOUND); this pins the single finding.
