# Phase: Exchange Wars — Phase 18r: Adversarial review of 18g–18q (the autonomous core-math fixes) (Brick 278)

**Started:** 2026-06-13
**Hat:** Reviewer (overdue quality gate — independently verify autonomous core-math changes)
**Goal:** Run the periodic adversarial review over the ~10 bricks since the last independent review (18b
covered 18c–18f), prioritising the CORE-MATH changes I made via the loop — 18o (`combatForecast` folds in
`hitChance`, wide blast radius via `diveReadiness`), 18p (`bidWalk` nets the sell tax), 18g (`deathRecap`
keepN) — plus the new 18q combat keydown listener.
**Done condition met:** verdict recorded; any findings closed (none — SOUND).

## Why this brick
The recent run included math I changed autonomously that predicts/mirrors the engine (a forecast model, a
money calc, a death-loss split) — exactly the kind of change that most needs an independent check, and ~10
bricks had accumulated since the last one (past the ~5 cadence). The Draft-Review-Merge gate exists to catch
a wrong autonomous "fix" before it compounds.

## Verdict — SOUND, zero findings
The reviewer verified all three core-math fixes against engine ground truth: `hitChance` (game.ts) is
byte-identical to quest.ts:410; the engine gates each hit on `rng.chance(hitChance)` then `damage()`, with
dragonfire `+ceil(atk/2)` INSIDE the landed-hit block — so `hitChance·(expectedHit+foeHitBonus)` models it
correctly; no div-by-zero (DPR ≥ 0.15·1); `diveReadiness` stays monotonic. `bidWalk.net` per-fill flooring
matches `paySeller` (exchange.ts:73-74, called per matched bid at :166) + the self-skip matches. `deathRecap`
keepN sort/split is identical to `expeditionDeath` (commands.ts:165), and `view.upgrades['deathWard']` is the
right source. 18q's `f` listener gates correctly (no off-tab/embark/typing fire, no collision with the embark
listener). Crucially, the new tests are real regression pins (the accuracy test FAILS under the old
no-accuracy formula; the per-fill tax test distinguishes 98 vs 97; the keepN test distinguishes 3 vs 5) — not
tautologies. No engine reach. **The predictor seam (18g/18o/18p) is now INDEPENDENTLY verified, not just
self-verified.**

## Scope (in)
- Documentation only: the review record in NEXT_STEPS + a FINDINGS checkpoint

## Scope (out)
- No code change (verdict SOUND — nothing to fix); no engine change → no redeploy

## Subsystems touched
- NEXT_STEPS.md, FINDINGS.md (review record)

## Gates
- [x] review run over 18g–18q; core-math fixes verified vs engine ground truth; verdict SOUND, zero findings
- [x] no code change → typecheck/UI suite/e2e unaffected (450 unit / 11 e2e remain green from 18q)
- [x] no engine change, no redeploy (batch stays 12b+13d+13e+13f+13r+14j)

## Open questions
- None — the seam audits (actionability arc, predictor reference-verify) are complete + independently reviewed.
