# Next Steps

Live: https://kalilinux1993.github.io/exchange-wars/ · repo github.com/kalilinux1993/exchange-wars
Deploy: push to main → CI (typecheck + vitest + e2e) auto-deploys Pages. Verifier: `npm run build:fn` + `npx supabase functions deploy verify-score --project-ref chynnshtcjclphlazkmv` (needs SUPABASE_ACCESS_TOKEN) — required in the same phase as any replay-affecting engine change.

> **⚠️ VERIFY-SCORE REDEPLOY PENDING (12b + 13d + 13e + 13f + 13r + 14j).** Six replay-affecting engine changes are live but the verifier edge function is NOT yet redeployed: (1) 12b — Field Forge / Skarn / Blood Altar content; (2) 13d — clerk gear-safety (the clerk no longer sells player-bought goods); (3) **13e — equipment manager** (equip/unequip commands + worn gear in `deriveStats`); (4) **13f — `equipBest` command** (one-click auto-equip best usable piece per slot); (5) **13r — `equipBest` tie-break** (stat-ties resolve by smaller itemId; stat-identical, so worth is unchanged); (6) **14j — `netWorth` counts equipped gear** (worn gear now valued at liquidation, so equipping is worth-neutral; CHANGES the leaderboard score for anyone who equips). `engine.js` in `supabase/functions/verify-score/` is rebuilt + committed (covers all six). **One command:** `npx supabase functions deploy verify-score --project-ref chynnshtcjclphlazkmv` (needs `SUPABASE_ACCESS_TOKEN`). Impact until then: dormant board (1 test row), soft worth-discrepancy only — no hard rejects.

Through brick 59 (10g): RPG layer (8 regions, stats/xp, brews, bounties, sellsword, death ward, 8 event faces), OSRS HUD (map/paperdoll/combat-scene with monster silhouettes + gear-reactive fighter/combat-level/titles), trading cockpit (movers/watchlist/alerts/pulse/sparkline/almanac/abort-all), robustness (error boundary + corrupt-save quarantine), art pipeline. Details in `.phases/` + DEV_GUIDE consolidations (8h–9f, 9g–9w, 9y–10g) + FINDINGS #1–#93.

## Queued candidates (rough priority)
- **ART status**: skill icons (9n), item category icons (10o), monster portraits in the bestiary (10p — shared MonsterBody) all original SVG. Remaining: region icons on the map; or Jesse drops game-icons.net (CC BY)/CC0 SVGs (same filename overrides item-*/skill-* originals). Auto-fetch impossible (WebFetch strips SVG paths).
- **HUD depth**: ~~"train to unlock" gear hints~~ DONE 10w; ~~per-slot item ICONS~~ DONE 11k; ~~region danger readout~~ DONE 11l; ~~effective combat stats (deriveStats)~~ DONE 11m; ~~per-axis danger colour (foe stat red when it beats yours)~~ DONE 11n; ~~embark combat forecast (≈rounds to kill vs ≈rounds to fall, favored/risky)~~ DONE 12j (`expectedHit`/`combatForecast`); ~~live in-combat forecast (push/flee read at current hp, counts dragonfire)~~ DONE 12o (`combatForecast` `foeHitBonus`). ~~foe stats in combat (atk/def danger-coloured, know-your-enemy)~~ DONE 13z: the combat readout shows the foe's ⚔/🛡 red/green vs your effective stats + a 🔥 mark, matching the embark danger lens. ~~note leech (gp-race not hp-race)~~ DONE 14a: a "💧−N gp/round" red marker on the combat foe line for Abyss leech foes (drains loot every round — finish fast/extract). ~~surface it in the region danger read too (pre-embark)~~ DONE 14b: `regionDanger` gains a `leech` field; the embark danger read shows "💧 drains loot" for the Abyss. Leech now surfaced pre-embark + in-fight. ~~bestiary combat stats~~ DONE 14c: met-monster Bestiary rows now show ⚔/🛡/hp (+ 💧leech) — a planning reference matching the combat/embark threat reads. The threat-surfacing arc (13z→14a→14b→14c) is complete across all three surfaces. The embark-decision panel (danger ⟷ your battle power ⟷ a rounds forecast) is now complete, and the fight itself has a live push/flee read. ~~pre-embark preparedness check (antifire for fire regions, food when risky)~~ DONE 12p (`embarkPrep`); ~~one-click "+ pack" fix chip beside each warning~~ DONE 12q (`EmbarkWarning` kind + draft bump). ~~a "prep me" button that fixes ALL warnings at once~~ DONE 13m: an embark-screen "⚑ prep me" button packs one of each DISTINCT owned fix (deduped — antifire/food can share super-antifire), shown only with ≥2 distinct fixes. UI-only, no redeploy. Next: "prep me" could top food to the forecast's rounds-to-fall depth (not just 1). ~~surface the at-risk loot on the extract action~~ DONE 13o: the dive's extract button reads "extract · bank N gp" (rise-green) when loot is at stake — the push-your-luck payoff on the action itself. UI-only, no redeploy. ~~an hp-aware "odds of surviving the next push" read to pair with the at-stake loot~~ DONE 13p: a mid-dive "push read" — `combatForecast` at CURRENT hp vs the region's hardest foe (dragonfire-aware), favored/risky, nudging "bank your haul?" when wounded with loot at stake. UI-only, no redeploy. ~~a heal-aware variant (count packed food into rounds-to-fall)~~ DONE 14o: `healFromPack` + a "🍖 food +≈N" cushion clause on the push read (verdict stays at RAW hp so food can't silently flip risky→favored; "bank?" nag dropped when food would win the race). UI-only, no redeploy. ~~show the TYPICAL foe alongside the hardest~~ DONE 15a: `regionTypical(region)` (pool average, elite excluded) + a "· usually favored/risky" tag on the push read, shown only when the typical verdict differs from the hardest (no over-scare in elite regions). UI-only, no redeploy. Next: an overheal-exact cushion. ~~rest heal-ETA~~ DONE 13x: `healEta(hp, max, regenTicks)` (pure) + "≈N ticks to heal" on the CharacterPanel hp line (wounded + out-of-field gated) — names the rest cost for the rest-vs-embark-hurt call. ~~a "rest to full" one-click fast-forward of exactly `restEta` ticks~~ DONE 13y: button beside the heal ETA (onRest threaded App→ExpeditionPanel→CharacterPanel). Readout→action loop complete. Next: richer paperdoll (per-item silhouettes); inventory/equipment grid. ~~combat level-up celebration~~ DONE 13v: `leveledUp(prev, now)` (pure) + a "⚔ Attack up!" toast the moment a combat skill ticks over (lazy-init ref, fires below deeds in the toast-priority order); ~~also flash the CharacterPanel skill cell~~ DONE 13w: the leveled skill cell pops gold (OSRS-style `@keyframes skillpop`), reusing `leveledUp` so toast + flash agree. Level-up feedback now complete (notify + confirm). ~~game-swap false-celebration edge~~ FIXED 14e (self-found): the level-up toast + skill flash re-baseline on a game/agent swap (cloud adoption / restart), so adopting a higher-level save no longer fires a false "level up!". Keyed to object identity (engine mutates the agent in place on a real level-up). ~~other mount-lazy refs (daily-record) may share the latent class~~ FIXED 14f: the daily-record celebration now re-baselines on cloud-save adoption too (it already did on restart; cloud was the gap) — closes the mount-lazy-celebration swap class. Durable follow-up: centralize "what to reset on a game swap" so the next celebration ref can't miss a swap site. ~~upgrade-shop affordability~~ DONE 13s: UpgradeShop shows the spendable purse + a "need +X" shortfall on each unaffordable upgrade (the spend lens, like 13l flips). Next: an ETA-to-afford from the recent gp/min rate (declined: only NET-WORTH history exists, not gp — a net-worth-rate ETA would mislead for gp-cost upgrades; needs a parallel gp-history first). ~~region-themed combat atmosphere~~ DONE 14u: `arenaTheme(regionId)` + a muted per-region gradient backdrop on the CombatScene (verdant plains → ember Maw → void Abyss) so the descent is FELT mid-fight, not just labelled; palettes kept dark so the readout stays legible. UI-only, no redeploy. Next (atmosphere): the RegionMap trail/nodes could carry the same per-region tint; a subtle region tint on the embark panel. **Note: `expectedHit` mirrors the engine damage formula (quest.ts:394) — update it if that formula moves.**
- **Feedback/idle**: ~~earning-rate cue~~ DONE 10x; ~~compact big-number formatting~~ DONE 10y (`fmtCompact`, masthead aggregates). ~~tiny worth sparkline beside the rate~~ DONE 11h; ~~open-position cost basis~~ DONE 11i; ~~worth ETA projection (≈Xm to next round number)~~ DONE 11w (`nextRoundTarget`). ~~extend `fmtCompact` to the away-bar gp gain + offline rate~~ DONE 11x (`offlineRatePerMin`). ~~"while you were away" market headline (the world kept living)~~ DONE 14y: `biggestMover(before, after)` (pure, ≥3% floor) + a "📈/📉 {item} ±X% ({start}→{end})" line in the away-bar; the pre-away prices are captured for free from the `playerView` `planOfflineProgress` already computes. UI-only, no redeploy. Next (away): your fills while away (data exists in `game.fills`); events that ended while away (newsLog with move%). Persist LIFETIME per-item P&L + cost basis (replaces the 50-fill "recent" window — meatier: a persistent FIFO lot structure on Game + normalizeGame rebuild-from-fills).
- **Trading**: ~~suggested-flip margin line~~ DONE 10j; ~~fair-value band~~ DONE 10m; ~~market-row sparkline~~ already built; ~~"best flips now" ranked strip~~ DONE 10v; ~~realized profit-by-item~~ DONE 11b; ~~flip return-on-cost % + buy limit on rows~~ DONE 11c (declined realizable-profit *ranking* — needs an order-flow model the UI can't verify). ~~one-click "place this flip" (prefill buy @ bid+1)~~ DONE 11d; ~~contract premium % + ready flag on the Quartermaster board~~ DONE 11y (`contractPremium`). ~~order-book liquidity bar~~ DONE 11f; ~~open-position cost basis~~ DONE 11i; ~~persist LIFETIME per-item P&L + cost basis (FIFO trade book)~~ DONE 11o; ~~trading scorecard headline (realized + paper)~~ DONE 11p; ~~flip consistency (hit-rate + worst item)~~ DONE 12n (`tradeRecord`); ~~one-click sell your whole position~~ DONE 11u; ~~Open Positions panel — every held item marked to market, unrealized P&L + %, click-to-load~~ DONE 12c (`heldPositions`); ~~portfolio concentration / allocation bar (risk lens — biggest exposure, red >50%)~~ DONE 12k (`positionConcentration`); ~~wealth composition by liquidity (cash / buy-offers / goods + at-risk loot)~~ DONE 12m (`worthBreakdown`/`WealthPanel`). ~~"average down" helper (blended cost preview when adding to a position)~~ DONE 12d (`blendBuy`); ~~break-even sell hint (floor-tax-exact sell floor + below-break-even warning)~~ DONE 12h (`breakEvenSell`). The ticket's pre-trade instrumentation is now symmetric (buy: how it reshapes cost basis ↔ sell: the price that clears it). ~~return on starting stake (am-I-up headline)~~ DONE 13k: `returnOnStake(startGp, worth)` (game.ts, pure) + a ↑/↓ gp+% badge in the WealthPanel header (green/red). Honest scoping: lifetime vs the original stake (startGp persists across reloads), NOT session-scoped. UI-only, no redeploy. ~~an **affordability lens** on TopFlips ("flips I can do now" given gp)~~ DONE 13l: `flipAffordability(buy, gp, limit)` (pure, in TopFlips.tsx) → per-row ×N badge (✕ + dim when unaffordable) + a "fits purse" filter that surfaces the best affordable flip. Honest scoping: a capital/limit cap (units you can OPEN), NOT a realizable-profit claim (depth is unknowable — that ranking stays declined). 13t: a "GE" tag now marks flips you can afford but the GE buy limit caps (gp to spare) — the "more capital won't help here" signal; `limited` flag tightened (zero-limit ≠ capped) per an adversarial review that found the 4 new money helpers otherwise SOUND. UI-only, no redeploy. Next idea: surface the scorecard/paper-P&L in the masthead; a sort toggle on Open Positions if the held list gets long; ~~tint the sell price input green/red around the break-even line~~ DONE 13q (`input.belowbe` reds the price field when a sell is below `breakEvenSell` — loss-prevention at the input); ~~**highlight underwater positions** in PositionsPanel~~ DONE 13n: `underwaterSummary` (pure) + a "⚠ N underwater −X paper" callout + a red left-edge on losing rows (they sort to the bottom, so the aggregate + tint surface them without scrolling). UI-only, no redeploy. Remaining: ~~a per-flip **round-trip trade journal**~~ DONE 13u: `recentFlips(fills, tax)` (pure FIFO-match, keeps per-flip detail the tradeBook aggregates away) + a "flips" mode on TradeFeed (tape/mine/flips). Window-scoped (FILLS_CAP=50); full history would need a persisted log. UI-only, no redeploy. Remaining: ~~a session-scoped (this-load) P&L via a captured baseline (distinct from 13k's lifetime figure)~~ DONE 14r: `sessionPnL(worth, baseline)` (pure) + a "this session ↑/↓ X (±Y%)" line in the WealthPanel; the baseline lives in App (always-mounted, so it survives tab switches) keyed to game identity (resets on a swap) — the unmount-sibling of the 14e/14f swap-guard class. UI-only, no redeploy. ~~A "cut losers" one-click sell on an underwater row~~ DONE 14s: a two-tap (arm→confirm) "✂ cut" chip on underwater PositionsPanel rows that market-sells the whole position at bestBid via the existing `place` command (gated on a new optional `onCommand` prop; `stopPropagation` so it doesn't also load the row). UI-only, no redeploy. ~~sortable flip-margin column on the market table (full-market lens vs TopFlips' top-4)~~ DONE 14d: `flipMargin` per row + a sortable "margin" column (reuses rankFlips' math; mobile-verified). ~~realized recent volatility readout (live counterpart to the static volatility tier)~~ DONE 14p: `priceSwing(recentPrices)` (pure) + a "recent lo–hi · swing X% · 🟢 steady/🟡 choppy/🔴 wild" line under the ticket sparkline — flags when a spread is too choppy to trust before both legs fill; reuses the sparkline's trade window, UI-only. Next idea: realized-swing as a sortable MarketTable column; std-dev/OHLC for power users. **Trading is deep — keep pivoting across systems (RPG content / progression / robustness / retention) for breadth between trading bricks.**
- **Robustness**: ~~UI to restore/export a quarantined save on next boot~~ DONE 10u (boot recovery bar: download/discard/dismiss). ~~warn before the manual new-game "start" wipes a run with progress~~ DONE 14x: `restartStakes(game, worth)` (pure) + a gated "⚠ wipes this run — {worth} net · {N} deeds" note in the seed form (shown only when deeds>0 or worth>startGp — no alarm on a fresh run); matches the challenge path's existing warning. UI-only, no redeploy. Remaining: guard monsterById/itemDef at UI read sites; 100k-tick determinism gate in a CI-only suite.
- **RPG content**: ~~Field Forge event~~ SHIPPED 11e; ~~Skarn elite (wilderness_ruins)~~ SHIPPED 11g; ~~Blood Altar event (hp→atk, forge's inverse)~~ SHIPPED 11j — **all merged to main in 12b (PR #1); verify-score redeploy PENDING (see header)**. more event faces (each bends a still-untouched system — FINDINGS #64); a 9th region (needs a NEW resource to extort); ~~attack brew~~ DONE 10n (goading → +10 atk/dive, the offensive counterpart to bastion); encounter depletion (only if an audit shows the TAS tail needs bounding). Idea: named elites for the other shallow regions (0–3); a defensive brew/altar (gp/hp → def) to round out the boost matrix.
- **Side-boards**: ~~contract premium % + ready flag~~ 11y; ~~bounty progress bar + reward-per-kill~~ 11z. Both decision-instrumented now.
- **Alerts**: ~~buy-below~~ 10b; ~~sell-above (take-profit)~~ 12a (`alertHit`, symmetric); ~~resting-fill notification (toast when your resting order fills during live ticks, 1×-gated)~~ DONE 13b (`fillSummary`, `recordFills` returns fresh fills). The price-alert + fill-feedback system is complete (enter, exit, and fill).
- **Progression**: ~~progress bars on the closest-unearned deeds (was bare % text)~~ DONE 12z (`.deedbar`, MilestonesPanel) — progression surfaces now visually consistent with the bounty/conquest/allocation bars. 10i region-mastery xp; 10k sellsword haul counters; 10l offline sellsword summary in the away-bar; ~~lifetime records profile~~ DONE 10z (RecordsPanel in the Hall); ~~timestamped deeds (🏅 latest · Nm ago)~~ DONE 11s; ~~deed markers on the Fortune chart~~ DONE 11t (`WorthChart` milestones prop). ~~gp sink that aids raiding~~ SHIPPED 11e (Field Forge: burn 300gp → +8 atk this delve); ~~per-region monster-mastery completion codex (👑 when a region's whole roster falls)~~ DONE 12f (`regionMastery`/`ConquestPanel`, Hall); ~~persistent Delve Log — per-expedition history (region/kills/loot/survived-died), latched at end~~ DONE 12i (`summarizeDelve`/`DelvePanel`); ~~click a Delve Log row to re-raid that region (jump to Adventure tab)~~ DONE 12l (regionPick nonce pulse); ~~raid risk/reward totals (loot banked vs lost) in the Delve Log header~~ DONE 12s (`raidTotals`). More: ~~flash the picked region on the RegionMap~~ DONE 14t (a 600ms `@keyframes mapflash` pulse on the selected node when `selected` changes — e.g. a Delve Log "raid again" jump; skip-mount so the static start doesn't strobe; UI-only); ~~a "realm conquered" deed when all 8 regions hit 👑~~ DONE 14q (`realm-conquered` milestone, reuses `regionMastery` for the done-check + an X/8 progress bar; UI-only); ~~per-region roster glyphs (lit/dim) for which-foe-is-left~~ DONE 14n (ConquestPanel rows now show each region's roster as lit/dim `MonsterGlyph`s — dim = the foes left to hunt; reuses `regionRoster` + `killsByMonster`, UI-only); final-region (Abyss) mastery hook.
- **Social/retention** (daily loop CLOSED — ember + nudge share one "day" definition): ~~daily shared seed~~ 10q; ~~"today" badge~~ 10r; ~~streak counter~~ 10s; ~~streak-at-risk nudge~~ 10t; ~~"you" highlight + rank on the Sprint Board~~ 11q (`myRank`); ~~"Daily Board" framing when seed == dailySeed()~~ 11r. ~~per-day personal best ("🏁 daily best")~~ DONE 12e (`recordDailyBest`/`dailyBestView` — masthead 🏁 tag, ▲ at a new peak, persisted); ~~"🎉 new daily record!" celebration toast on beating your prior best~~ DONE 12w (`beatRecord`). The daily↔leaderboard loop is now closed and the solo high-score hook is in (passive tag + active celebration). Leftover daily polish (low priority): a one-time "🎉 new record!" toast vs the always-on ▲; "today's board" framing on the leaderboard view.
- **Input/UX**: ~~keyboard shortcuts (1/2/3 rooms, p pause, ? help)~~ DONE 11a; ~~market-row nav (j/k or ↑/↓), matching the displayed sort/filter~~ DONE 12g (`nextRowIndex`, MarketTable `active`-gated window listener); ~~ticket side hotkeys (b/s pick buy/sell)~~ DONE 12t. The trade loop is now keyboard-driven end to end (j/k pick item → b/s pick side → type → Enter submits); ~~scroll the keyboard-selected row into view on long lists~~ DONE 12v (`scrollIntoView` block:nearest + typeof guard). ~~keyboard-drive the adventure embark too~~ DONE 14w: EmbarkPanel takes an `active` prop + a window keydown listener — ←/→ move the selected region (clamped to unlocked), Enter embarks via a shared `doEmbark()`; gated to the active Adventure tab and guarded against typing. UI-only, no redeploy. Next: quick-select digits for top market rows (mind the 1/2/3 room-key collision).
- **Onboarding**: ~~Getting Started first-steps checklist (auto-hiding)~~ DONE 11v (`firstSteps`); ~~help overlay documents j/k + b/s keyboard shortcuts; start-gp sourced from `HUMAN_START_GP`~~ DONE 13a. Next: contextual one-liner hints in empty panels for brand-new players.
- **Jesse-gated**: prestige/rebirth loop; a deeper art-direction steering pass. **⚠️ ACTION REQUIRED — verify-score redeploy is PENDING for the 12b content merge (Field Forge / Skarn / Blood Altar changed the replay). One command when Jesse is present: `npm run build:fn` (already done — engine.js is current in the repo) then `npx supabase functions deploy verify-score --project-ref chynnshtcjclphlazkmv` (needs `SUPABASE_ACCESS_TOKEN`). Until then, scores submitted with the new content replay against the OLD verifier → a soft worth-discrepancy (NOT a hard reject; the verifier takes only `{handle,seed,log}`). Board is dormant (1 test row) so blast radius ≈ nil. Future engine content: still blocked on the token to redeploy in the same phase.**
  - **Parked engine content (do WITH Jesse — needs review + the redeploy above):** named elites for shallow regions 0–3 (incl. canonical OSRS bosses Obor for hill giants / Bryophyta for moss giants); a defensive brew/altar (gp/hp → def) to round out the boost matrix; a 9th region (new resource to extort). ⚠️ **Adding an elite to a region inserts an `rng.chance(ELITE_CHANCE)` draw in `rollEncounter` — `lumbridge_plains` (region 0) is heavily fixed-seed-tested in `expedition.test.ts`, so expect to re-pin many deterministic assertions (FINDINGS #152). Put new elites in lightly-tested regions first, or budget for the test reconciliation.**

## Decided / dropped
- **Gear durability — DROPPED** (FINDINGS, 9g): raiders already pay spread + death burns + tick cost + consumable tickets; a durability tax re-punishes the validated capital loop for no asked-for benefit.
- **Tier-3 clerk "trades events too" — DROPPED**: clerks farm neither events nor exotics (FINDINGS #27/#35).

## Quality
- **Adversarial review — pure helpers (12u, brick 125):** the session's ~25 pure helpers reviewed for correctness/edge-cases — verdict **SOUND**, edges pinned as regression tests.
- **Adversarial review — effects/listeners (brick 128):** found + FIXED a HIGH bug — the sellsword autopilot shares `agent.expedition`, so its dives polluted the Delve Log (12i) + fired false player "You died" toasts. Fixed 12x (`bySellsword` snapshot gate); 12y added the render-side banner + toggle haul. Also fixed the celebration refs not resetting on restart. Remaining notes pre-existing/cosmetic (Space vs shortcut BUTTON-guard inconsistency) — non-blocking.
  - **Jesse-present follow-up:** an engine-side `bySellsword` flag on `ExpeditionState` would let the UI distinguish ownership cleanly (vs the `agent.sellsword` heuristic) and DISABLE player controls during a sellsword dive — a small replay-affecting change to batch with the verify-score redeploy.

## Trading safety
- ~~"sell the spoils" bulk button dumped GEAR too (could liquidate the raiding kit)~~ FIXED 12r (`lootSpoils` — bulk keeps gear; per-item sell still sells anything). Optional follow-up: a "sell gear too" override.
- ~~**CRITICAL (user-reported): the autoFlip CLERK sold player-bought gear before it could be equipped → satchel looked "always empty"**~~ FIXED 13d (engine: the clerk only sells items it has a cost `basis` for = its own flip-stock; won't start a flip on a player holding either). Benchmark/sim byte-identical (hash `fe75df57`). **Replay-affecting → needs the verify-score redeploy above.**

## OSRS equipment manager — ~~SHIPPED 13e~~ · Adventure-tab manager ~~SHIPPED 14h~~
**14h (Jesse-requested):** a `GearManager` on the Adventure tab — owned gear with its +stats, level requirement, and the upgrade verdict vs what's worn (↑+N / ↓-N / no gain / 🔒 train / ✓ worn), plus equip/unequip/equip-best. Reuses `gearDelta` (13h); UI-only, no redeploy. ~~sort owned gear by biggest upgrade~~ DONE 14i (usable-first, then delta desc, then name). Follow-ups: paperdoll-grid for the equipped section; sort owned gear by slot. ~~a "best affordable upgrade on the market" advisor~~ DONE 14v: `bestAffordableUpgrade(worn, lvls, gp, markets, inventory)` (pure, reuses `gearDelta`) + a "💰 best buy: {item} ⚔/🛡+N · {price} gp" line in the GearManager (affordable + usable + not-owned only; optional `view` prop). UI-only, no redeploy. ~~make it click-to-buy (jump to the Exchange + select the item)~~ DONE 14z: a "→ buy" chip on the best-buy line calls `onBuy(itemId)` (App: `setSelected` + `pickRoom('exchange')`, threaded App→ExpeditionPanel→GearManager); loads the ticket, doesn't auto-buy. UI-only, no redeploy.

**14m (Jesse-requested):** the embark "Plan a Dive" flow (region pick, danger/forecast read, prep warnings, Pack & Equip, loadouts, EMBARK) extracted from the bottom of the tall left `ExpeditionPanel` into its own `EmbarkPanel` in the Adventure tab's RIGHT column — Jesse pointed at the empty right side. Pure move (engine/commands/deriveStats byte-identical); ExpeditionPanel keeps only adventurer status + Bestiary and exports `regionDanger` for the new panel. UI-only, no redeploy.


~~The user wants manually-bought gear equipped via an OSRS-style equipment manager.~~ DONE 13e: `agent.worn` per slot + `equip`/`unequip` commands (inventory↔worn, conservation-counted, level-gated) + `deriveStats(pack, lvls, worn?)` where worn OVERRIDES the pack per slot (absent worn = byte-identical old behaviour → sim hashes unchanged) + worn gear is safe on death + an Equipment section in PlayerPanel (equip chip per gear stack, unequip per slot). **Replay-affecting → in the verify-score redeploy above.** ~~an "auto-equip best" one-click~~ DONE 13f: `equipBest` command — for each slot, equips the best USABLE owned piece (satchel OR already worn; level-gated; first-seen tie-break matching `deriveStats`), displaced piece returns to the satchel, no-op when nothing improves, rejected mid-expedition + an "equip best" button in PlayerPanel. Benchmark/sim byte-identical (hashes unchanged). ~~the Adventure-tab paperdoll ignored `worn` (showed satchel-best preview, disagreed with the manager + combat)~~ FIXED 13g: `equipped(inv, lvls, worn?)` + `deriveStats(inv, lvls, agent.worn)` in CharacterPanel now reflect ACTUAL equipped gear (worn overrides the inventory pick, mirroring the engine) — figure, "in battle" effstats, and the Exchange-tab Equipped list all agree. UI-only, no redeploy. ~~the buy-upgrade decision was blind (satchel showed an equip chip with no "is this better?" signal — the user's ORIGINAL pain)~~ ADDRESSED 13h: `gearDelta(itemId, worn, lvls)` (game.ts, pure) + a per-stack badge in PlayerPanel — ⚔/🛡 `+N` green upgrade / red downgrade / 🔒 req when under-level / "no gain" sidegrade, governing stat vs what's worn. UI-only, no redeploy. ~~surface the same `gearDelta` on the buy ticket so upgrade value shows BEFORE you buy~~ DONE 13i: TradeTicket previews "equips as ⚔/🛡 ±N {stat} vs your {worn}" (+ "needs Atk N" when under-level) on a gear buy; levels threaded from App as an optional prop. UI-only, no redeploy. ~~a gear marker/filter on the MarketTable so gear is findable among the 128 commodities~~ DONE 13j: a ⚔/🛡 marker on every gear row (titled with slot + level req) + a "gear" track filter isolating the equippable subset; reuses the engine `GEAR` map as the taxonomy. UI-only, no redeploy. Remaining follow-ups: a richer paperdoll GRID (per-item silhouettes in slot layout) vs the current SVG figure + list; the "gear" track could sort by upgrade delta vs what's worn (best buy first); decide if deep-region death should risk worn gear (currently always safe).

## Old minor leftovers (non-blocking)
- Trades window: `shift()` → ring buffer if it ever grows beyond 512.
- Momentum traders bleed slowly (FINDINGS #12) — acceptable.
