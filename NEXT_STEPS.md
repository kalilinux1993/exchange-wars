# Next Steps

Live: https://kalilinux1993.github.io/exchange-wars/ · repo github.com/kalilinux1993/exchange-wars
Deploy: push to main → CI (typecheck + vitest + e2e) auto-deploys Pages. Verifier: `npm run build:fn` + `npx supabase functions deploy verify-score --project-ref chynnshtcjclphlazkmv` (needs SUPABASE_ACCESS_TOKEN) — required in the same phase as any replay-affecting engine change.

> **⚠️ VERIFY-SCORE REDEPLOY PENDING (12b + 13d + 13e + 13f + 13r + 14j).** Six replay-affecting engine changes are live but the verifier edge function is NOT yet redeployed: (1) 12b — Field Forge / Skarn / Blood Altar content; (2) 13d — clerk gear-safety (the clerk no longer sells player-bought goods); (3) **13e — equipment manager** (equip/unequip commands + worn gear in `deriveStats`); (4) **13f — `equipBest` command** (one-click auto-equip best usable piece per slot); (5) **13r — `equipBest` tie-break** (stat-ties resolve by smaller itemId; stat-identical, so worth is unchanged); (6) **14j — `netWorth` counts equipped gear** (worn gear now valued at liquidation, so equipping is worth-neutral; CHANGES the leaderboard score for anyone who equips). `engine.js` in `supabase/functions/verify-score/` is rebuilt + committed (covers all six). **One command:** `npx supabase functions deploy verify-score --project-ref chynnshtcjclphlazkmv` (needs `SUPABASE_ACCESS_TOKEN`). Impact until then: dormant board (1 test row), soft worth-discrepancy only — no hard rejects.

> **🤝 AWAITING JESSE — the session-19 handoff (all remaining substantive work is gated on these; consolidated here so they're not scattered across the Quality notes below):**
> 1. **Verify-score redeploy** (the one above) — also the unlock for the spec'd content batch in `NEXT_CONTENT.md` (shallow elites · defensive altar event · 9th region). Run `npm run build:fn` + `npx supabase functions deploy verify-score --project-ref chynnshtcjclphlazkmv` (needs `SUPABASE_ACCESS_TOKEN`).
> 2. **Cloud backend deploy** (per memory) — paste `supabase/schema.sql` + set the Auth Site URL, so cloud saves/leaderboard go live (client is ready; the 19w/19x/19y data-loss fixes ship regardless).
> 3. **Three flagged PRODUCT decisions** (I declined to guess — each is a balance/UX call): (a) **gear-stat baseline** — effective-stat displays use `deriveStats(inventory,…)`, `gearDelta` compares vs `worn`, combat uses `pack`; which baseline should the upgrade preview use? (19o) (b) **cloud conflict policy** — a *barely-played* local vs a *rich older* cloud is latest-wins (respects an intentional restart); prefer-more-progress instead? (19w) (c) **import warning** — should a *valid* import warn before replacing a run-with-progress (new-game 14x parity)? (19x)
>
> **Session 19 summary (2026-06-13, bricks 19e–19z, all live + green):** 10 real bugs fixed via two audit veins — engine-mirror math (forecast under-count, bid/ask tax, deathRecap) and data-integrity (sync feedback, offline catch-up, cloud merge, import, local load); a combat survivability ladder + symmetric trading-instrument suite; distribution/data-safety; DEV_GUIDE consolidation (brick 300); leaderboard anti-cheat audit + expedition-conservation audit (both SOUND); full gate set verified green (624 tests, sim invariants OK, balance healthy). Codebase is feature-complete + audited; the items above are the only path to more substantive work.
>
> **Session 20–21 summary (2026-06-13, bricks 20e–21l, all live + green → 659 tests):** 12 UI features across every system, then a review + a re-verify. Features: a11y sort-state `aria-sort` (20e) + keyboard-operable headers (20f); shop afford-ETA / sell-to-afford (21a); an always-available **handle setter** (21b — closed a reachability gap: the social features referenced a handle with no way to set it offline); the **outgrown-farm** push-deeper nudge (21c); symmetric position **take-profit** (21d) + a **stale-offer dead-capital aggregate/abort** (21e); two replay-inert deeds — **Untouchable** (10-dive clean streak, 21f) and **Profiteer** (100k realized flip profit, 21g); a Help-overlay refresh teaching them all (21h); a live **event price-move %** on the chip (21i) and the ticket decision-point (21l); a **dive survival report card** in the Almanac (21j). Then (21k) an **adversarial review** caught + fixed a real cross-render take/cut `armed`-state bug (the rest verified SOUND), and (21l) a **full market-sanity re-verify**: 659 tests green, sim invariants OK on seeds 11/42/1337, **seed-42 hash `fe75df57` UNCHANGED** — 13 UI-only bricks provably did not perturb engine determinism/conservation. All UI-only → no redeploy. **The safe autonomous UI-feature space is now saturated; the three gated items above are the path to substantive work.**
>
> **One flagged UI candidate (deferred — needs Jesse, not autonomous):** a **margin / flippability ALERT** (the 5th alert type, after buy-below/sell-above/cheap-band/rich-band) — high value for flippers ("ping me when X becomes worth flipping") but needs a THRESHOLD-MODEL decision (per-item absolute gp vs a single global gp vs %-of-price vs band-relative — a global absolute threshold is crude across items of different price scales) AND it touches the bug-prone alert machinery (fired-set + swap-guard + toast priority, cf. the 16e toast-priority bug). Worth a focused session with your input on the model; left un-built rather than shipped crude.

Through brick 59 (10g): RPG layer (8 regions, stats/xp, brews, bounties, sellsword, death ward, 8 event faces), OSRS HUD (map/paperdoll/combat-scene with monster silhouettes + gear-reactive fighter/combat-level/titles), trading cockpit (movers/watchlist/alerts/pulse/sparkline/almanac/abort-all), robustness (error boundary + corrupt-save quarantine), art pipeline. Details in `.phases/` + DEV_GUIDE consolidations (8h–9f, 9g–9w, 9y–10g) + FINDINGS #1–#93.

## Queued candidates (rough priority)
- **ART status**: skill icons (9n), item category icons (10o), monster portraits in the bestiary (10p — shared MonsterBody) all original SVG. Remaining: region icons on the map; or Jesse drops game-icons.net (CC BY)/CC0 SVGs (same filename overrides item-*/skill-* originals). Auto-fetch impossible (WebFetch strips SVG paths).
- **HUD depth**: ~~"train to unlock" gear hints~~ DONE 10w; ~~per-slot item ICONS~~ DONE 11k; ~~region danger readout~~ DONE 11l; ~~effective combat stats (deriveStats)~~ DONE 11m; ~~per-axis danger colour (foe stat red when it beats yours)~~ DONE 11n; ~~embark combat forecast (≈rounds to kill vs ≈rounds to fall, favored/risky)~~ DONE 12j (`expectedHit`/`combatForecast`); ~~live in-combat forecast (push/flee read at current hp, counts dragonfire)~~ DONE 12o (`combatForecast` `foeHitBonus`). ~~foe stats in combat (atk/def danger-coloured, know-your-enemy)~~ DONE 13z: the combat readout shows the foe's ⚔/🛡 red/green vs your effective stats + a 🔥 mark, matching the embark danger lens. ~~note leech (gp-race not hp-race)~~ DONE 14a: a "💧−N gp/round" red marker on the combat foe line for Abyss leech foes (drains loot every round — finish fast/extract). ~~surface it in the region danger read too (pre-embark)~~ DONE 14b: `regionDanger` gains a `leech` field; the embark danger read shows "💧 drains loot" for the Abyss. Leech now surfaced pre-embark + in-fight. ~~bestiary combat stats~~ DONE 14c: met-monster Bestiary rows now show ⚔/🛡/hp (+ 💧leech) — a planning reference matching the combat/embark threat reads. The threat-surfacing arc (13z→14a→14b→14c) is complete across all three surfaces. ~~the foe's DROPS in combat (the reward half of fight/flee)~~ DONE 17w: a `.foedrops` line under the combat forecast shows the current foe's drops (likeliest first, top 3 + "+N more"), reusing `monsterById(...).drops` — the same table the Bestiary (per-monster) and embark loot read (15u, region aggregate) use, here for the SPECIFIC foe. Completes risk⟷reward in the fight. UI-only, no redeploy. The embark-decision panel (danger ⟷ your battle power ⟷ a rounds forecast) is now complete, and the fight itself has a live push/flee read. ~~a GLOBAL "how deep can I farm?" readiness read (the per-region forecast scans one region at a time)~~ DONE 16x: `diveReadiness(you, frontier)` (ExpeditionPanel, pure — the deepest unlocked region you're favored vs its `regionTypical` foe at full hp, -1 if outmatched; reuses `combatForecast`) + a "✓ ready / 📍 your safe depth is {region} / ⚠ outmatched" line atop the embark region-readout. Unlock ≠ ready; keys on the TYPICAL foe (farmability) where the per-region read keys on the HARDEST foe (worst case) — complementary. 4 units + a render line; tightened two pre-existing embark tests for the new "favored"/region-name text (#147/#150 collision class). UI-only, no redeploy. ~~tint/mark the recommended region on the RegionMap~~ DONE 17g: a green dashed "dive here" `.maprec` ring on the deepest-favored node (reuses `diveReadiness`, hoisted in EmbarkPanel so the text readout + the map ring share one computation; coexists with the gold selection ring by radius+hue). The readiness advice now maps text→node. UI-only, no redeploy. ~~Next: a "you've outgrown {region}" nudge when ready jumps past your usual farm.~~ DONE 21c: pure `outgrownFarm(recentRegionIdxs, safeDepth)` + a "🎯 you keep farming {region} — you're ready for {deeper}" embark line, fired only when your safe depth (`diveReadiness.ready`) is past a real recent-farm cluster (≥3 of last 6 dives in one region). Reads `game.delves` (no new prop); silent without dive history → no collision with the existing embark tests. 6 tests. ~~pre-embark wounded nudge (the forecast assumes full hp — warn + rest-to-full if you'd dive hurt)~~ DONE 15w: a "⚠ you're at {hp}/{max} hp — you'll dive hurt" warning + a one-tap "rest to full (≈N ticks)" on the embark screen when `agent.hp` is below max (reuses `healEta`/`REST_REGEN_TICKS`/`onRest=fastForward` from 13x/13y; the embark twin of the mid-dive push-read 13p). UI-only, no redeploy. ~~the REWARD side of the embark decision (loot upside, not just danger)~~ DONE 15u: `regionLoot(roster)` (pure — gp/kill range + distinct drops at best chance, likeliest first) + a "loot: ≈lo–hi gp/kill · drops: {icons}" line after the danger read, so the embark is a risk⟷reward weigh, not risk-only (the forward complement to 15c's historical `raidTotalsByRegion`). UI-only, no redeploy. Scoped to an honest range + drop list, NOT a speculative EV. ~~an item→source index ("where does this drop?") on the trading side~~ DONE 15v: `itemSources(itemId, monsters, regions)` (pure reverse-index — every monster that drops the item, with its region via `region.monsters`/`elite`, likeliest first) + a "🗡 farm: {monster} {chance}% · {region}" line on the ticket, gated to drop-items only (off every staple). Closes the trade↔farm cross-reference in both directions (15u = region→items, 15v = item→region). UI-only, no redeploy. ~~the farm line named the monster/region but offered no way to GO there~~ DONE 18m: a "hunt" button on the farm line jumps to the Adventure tab with the source's region pre-selected (App passes `onHunt={jumpToRegion}` — its 3rd caller after the Delve-Log raid-again 12l + bounty hunt 18f; reuses `itemSources.regionId`, no new helper). "Farm it instead of buying it." The trade→farm sibling of the bounty hunt. Render test. UI-only, no redeploy. ~~pre-embark preparedness check (antifire for fire regions, food when risky)~~ DONE 12p (`embarkPrep`); ~~one-click "+ pack" fix chip beside each warning~~ DONE 12q (`EmbarkWarning` kind + draft bump). ~~a "prep me" button that fixes ALL warnings at once~~ DONE 13m: an embark-screen "⚑ prep me" button packs one of each DISTINCT owned fix (deduped — antifire/food can share super-antifire), shown only with ≥2 distinct fixes. UI-only, no redeploy. Next: "prep me" could top food to the forecast's rounds-to-fall depth (not just 1). ~~an extract recap toast (the positive close to the death recap)~~ DONE 15n: a "🎒 Returned from {region}: banked N loot gp · M cleared" toast on a clean extract (fired before `onDelveEnd` so a record haul's "New best haul!" still wins on top; gated on packGp/cleared > 0). UI-only, no redeploy. ~~surface the at-risk loot on the extract action~~ DONE 13o: the dive's extract button reads "extract · bank N gp" (rise-green) when loot is at stake — the push-your-luck payoff on the action itself. ~~the push-read shows survival ODDS but never the CONSEQUENCE of dying~~ DONE 18e: an in-dive `.deathstakes` line (gated to packGp>0) renders `deathRecap` BEFORE death — "⚰ if you fall here: lose {gp} loot gp [+ N items] · keep {your 3 best}" — the consequence half of the push-vs-bank call (push-read 13p = odds). Reuses the exact recap the death toast uses (preview can't disagree with outcome); 2 render tests (shows w/ loot, hidden w/o). UI-only, no redeploy. UI-only, no redeploy. ~~an hp-aware "odds of surviving the next push" read to pair with the at-stake loot~~ DONE 13p: a mid-dive "push read" — `combatForecast` at CURRENT hp vs the region's hardest foe (dragonfire-aware), favored/risky, nudging "bank your haul?" when wounded with loot at stake. UI-only, no redeploy. ~~a heal-aware variant (count packed food into rounds-to-fall)~~ DONE 14o: `healFromPack` + a "🍖 food +≈N" cushion clause on the push read (verdict stays at RAW hp so food can't silently flip risky→favored; "bank?" nag dropped when food would win the race). UI-only, no redeploy. ~~show the TYPICAL foe alongside the hardest~~ DONE 15a: `regionTypical(region)` (pool average, elite excluded) + a "· usually favored/risky" tag on the push read, shown only when the typical verdict differs from the hardest (no over-scare in elite regions). UI-only, no redeploy. Next: an overheal-exact cushion. ~~rest heal-ETA~~ DONE 13x: `healEta(hp, max, regenTicks)` (pure) + "≈N ticks to heal" on the CharacterPanel hp line (wounded + out-of-field gated) — names the rest cost for the rest-vs-embark-hurt call. ~~a "rest to full" one-click fast-forward of exactly `restEta` ticks~~ DONE 13y: button beside the heal ETA (onRest threaded App→ExpeditionPanel→CharacterPanel). Readout→action loop complete. Next: richer paperdoll (per-item silhouettes); inventory/equipment grid. ~~the skill bars drew XP progress but with no value (tooltip was just "Attack 40")~~ DONE 18j: the skill tooltip now quantifies the bar — "Attack 40 · 62% to 41 · 1,240 xp to go" (maxed → "maxed (99)") — so "how close to the next gear-gating level?" is readable, not eyeballed; reuses the already-computed `into` + `xpForLevel`. 2 render tests. UI-only, no redeploy. ~~combat level-up celebration~~ DONE 13v: `leveledUp(prev, now)` (pure) + a "⚔ Attack up!" toast the moment a combat skill ticks over (lazy-init ref, fires below deeds in the toast-priority order); ~~also flash the CharacterPanel skill cell~~ DONE 13w: the leveled skill cell pops gold (OSRS-style `@keyframes skillpop`), reusing `leveledUp` so toast + flash agree. Level-up feedback now complete (notify + confirm). ~~game-swap false-celebration edge~~ FIXED 14e (self-found): the level-up toast + skill flash re-baseline on a game/agent swap (cloud adoption / restart), so adopting a higher-level save no longer fires a false "level up!". Keyed to object identity (engine mutates the agent in place on a real level-up). ~~other mount-lazy refs (daily-record) may share the latent class~~ FIXED 14f: the daily-record celebration now re-baselines on cloud-save adoption too (it already did on restart; cloud was the gap) — closes the mount-lazy-celebration swap class. Durable follow-up: centralize "what to reset on a game swap" so the next celebration ref can't miss a swap site. ~~upgrade-shop affordability~~ DONE 13s: UpgradeShop shows the spendable purse + a "need +X" shortfall on each unaffordable upgrade (the spend lens, like 13l flips). ~~Next: an ETA-to-afford from the recent gp/min rate (declined: only NET-WORTH history exists, not gp — a net-worth-rate ETA would mislead for gp-cost upgrades; needs a parallel gp-history first).~~ DONE 21a — the decline was wrong: it assumed dividing a CASH shortfall by a WEALTH rate (mismatched units). Correct framing — upgrades cost cash but cash is fungible with goods (liquidate), so time-to-afford keys on the NET-WORTH gap (cost − worth) ÷ `worthRate.perMin` (units match), and worth ≥ cost > gp becomes "💰 sell to afford" (you have the wealth, just not in cash). Pure `affordEta(cost, worth, worthPerMin)` → liquidate / eta / slow (no invented ETA when wealth is flat/falling); UpgradeShop shows the hint beside each "need +X". Reuses the EXISTING worthHistory/worthRate — no gpHistory series needed (the cleaner path the decline missed). 6 tests. UI-only, no redeploy. ~~region-themed combat atmosphere~~ DONE 14u: `arenaTheme(regionId)` + a muted per-region gradient backdrop on the CombatScene (verdant plains → ember Maw → void Abyss) so the descent is FELT mid-fight, not just labelled; palettes kept dark so the readout stays legible. UI-only, no redeploy. ~~the RegionMap trail/nodes could carry the same per-region tint~~ DONE 15t: a per-region colour halo behind each map node (reuses `arenaTheme`, rendered as a separate element behind the state-coloured circle so cleared/frontier/locked coding is untouched; locked nodes dimmer). The descent reads green plains → ember Maw → void Abyss on the map. UI-only, no redeploy. ~~a subtle region tint on the embark panel~~ DONE 15y: the embark "what you're getting into" block (flavor/danger/loot/forecast/wounded) sits in a `.region-readout` div with a faint per-region `arenaTheme` gradient (hex8 low-alpha, legible) — all three adventure surfaces (combat/map/embark) now share the palette. UI-only, no redeploy. The per-region atmosphere arc is complete. **Note: `expectedHit` mirrors the engine damage formula (quest.ts:414-415) — update it if that formula moves.** ~~reference-verify it~~ DONE 18o: verified in sync (damage roll bounds + def reduction match) AND found+fixed a bigger desync — `combatForecast` ignored the engine's `hitChance` (quest.ts:410) entirely, dividing hp by per-HIT damage as if every swing landed. Now folds accuracy in (per-round = `hitChance × expectedHit`, dragonfire bonus inside the foe multiply), so favored/risky verdicts + `diveReadiness` safe-depth account for misses. `hitChance` added to game.ts mirroring quest.ts:410, unit-pinned. UI-only (predictor only — engine untouched), no redeploy. ~~swept the sibling predictor `bidWalk`~~ DONE 18p: it returned GROSS sell proceeds while the engine taxes 2% per fill (`paySeller`) — the "sell @ bid" loot-dump overstated your receipt + contradicted the tax-aware ticket. Added `bidWalk.net` (per-fill floored tax, byte-equal to `paySeller`); the PlayerPanel now shows "realize ≈{net} gp" / "satchel, after tax". Test pins display==actual receipt (3,626). **Reference-verify seam (18g deathRecap keep-N · 18o forecast hitChance · 18p bidWalk tax) — the UI↔engine money/combat predictors are now all verified.** UI-only, no redeploy.
- **Feedback/idle**: ~~earning-rate cue~~ DONE 10x; ~~compact big-number formatting~~ DONE 10y (`fmtCompact`, masthead aggregates). ~~tiny worth sparkline beside the rate~~ DONE 11h; ~~open-position cost basis~~ DONE 11i; ~~worth ETA projection (≈Xm to next round number)~~ DONE 11w (`nextRoundTarget`). ~~extend `fmtCompact` to the away-bar gp gain + offline rate~~ DONE 11x (`offlineRatePerMin`). ~~"while you were away" market headline (the world kept living)~~ DONE 14y: `biggestMover(before, after)` (pure, ≥3% floor) + a "📈/📉 {item} ±X% ({start}→{end})" line in the away-bar; the pre-away prices are captured for free from the `playerView` `planOfflineProgress` already computes. UI-only, no redeploy. ~~your biggest-moving HOLDING while away (personal complement to the market headline)~~ DONE 15x: `heldMover(before, markets, inventory)` (= `biggestMover` over the held subset) + a "💼 your {item} ±X%" away-bar line, deduped vs `topMover`. UI-only, no redeploy. ~~deeds earned while away (the offline accrual crosses a milestone silently)~~ DONE 17a: an `awayDeeds` state captured from the boot + catch-up `checkMilestones` returns (after a pre-accrual latch that excludes deeds already achievable before the gap), surfaced as a "🏅 earned while away: {deeds}" away-bar line — the deed sibling of 16v's offline duel settle. 1 render test (clerk crosses first-offer during the gap; hired-help/dragon-slayer excluded). UI-only, no redeploy. Next (away): your fills while away is ENGINE-GATED (the 512-trade window evicts them before `recordFills`, per 15q — needs a per-player fill accumulator); events that ended while away is ENGINE-GATED too (sim.ts:143 prunes ended events from `world.events` every checkEvery ticks — needs an event-history accumulator, RULED OUT 17a-design). Persist LIFETIME per-item P&L + cost basis (replaces the 50-fill "recent" window — meatier: a persistent FIFO lot structure on Game + normalizeGame rebuild-from-fills).
- **Trading**: ~~suggested-flip margin line~~ DONE 10j; ~~fair-value band~~ DONE 10m; ~~market-row sparkline~~ already built; ~~"best flips now" ranked strip~~ DONE 10v; ~~realized profit-by-item~~ DONE 11b; ~~flip return-on-cost % + buy limit on rows~~ DONE 11c (declined realizable-profit *ranking* — needs an order-flow model the UI can't verify). ~~one-click "place this flip" (prefill buy @ bid+1)~~ DONE 11d; ~~contract premium % + ready flag on the Quartermaster board~~ DONE 11y (`contractPremium`). ~~order-book liquidity bar~~ DONE 11f; ~~watchlist decision board (flip margin + value-band per starred item)~~ DONE 15i: extracted `flipMargin` to game.ts (single source for the market column + watch row) + added a "flip +N · 🟢/⚪/🟡" readout to each WatchlistPanel row (reuses `flipMargin` + `valueBand`). UI-only, no redeploy. ~~a fired alert (🔔) only changed the row's text colour in place — easy to miss mid-list in a long watchlist~~ DONE 18l: extracted `isTriggered(m)` (the buy/sell `alertHit` check) and stable-sort the watchlist so triggered rows float to the top — a fired alert is now unmissable (completes alert→SEE→act). Stable sort = a row moves only when its alert fires/clears (not tick jitter, unlike a margin/price sort). Render test. UI-only, no redeploy. ~~open-position cost basis~~ DONE 11i; ~~persist LIFETIME per-item P&L + cost basis (FIFO trade book)~~ DONE 11o (NOTE: the FIFO book's realized tax was FIXED 18w — it floored the 2% sell tax PER UNIT, which is 0 below price 50, so cheap-staple flips booked ZERO tax and overstated profit ~22%; now per-fill `floor(price·qty·rate)`, matching the engine's paySeller, across `applyFillToBook` + `recentFlips`; 4 P&L tests corrected + a sub-50 regression added; the reference-verify seam's biggest catch, on the primary flip loop; and 18x VERIFIED `flipMargin` is qty-1-EXACT — its per-unit `floor(sell·rate)` matches the engine for a 1-unit flip (do NOT per-fill-ify it like the book; doc invariant + cheap-item test pin added). **Money-path reference-verify COMPLETE: bidWalk 18p + realized book 18w fixed to per-fill; combatForecast 18o + deathRecap 18g fixed; worthBreakdown + flipMargin verified sound. Flagged for Jesse: whether the margin column should show an at-scale `sell·rate` tax for bulk-flip realism — a deliberate re-ranking decision.**); ~~trading scorecard headline (realized + paper)~~ DONE 11p; ~~flip consistency (hit-rate + worst item)~~ DONE 12n (`tradeRecord`); ~~one-click sell your whole position~~ DONE 11u; ~~Open Positions panel — every held item marked to market, unrealized P&L + %, click-to-load~~ DONE 12c (`heldPositions`); ~~portfolio concentration / allocation bar (risk lens — biggest exposure, red >50%)~~ DONE 12k (`positionConcentration`); ~~preview that concentration at the BUY (pre-commit risk lens, not just post-hoc)~~ DONE 15r: `buyConcentration(view, itemId, addCost)` (pure — worth = cash + held goods at last price, unchanged by a buy which just swaps cash→goods; item share = held value + committed gp) + a "after fill: {item} ≈ X% of holdings ⚠" line on the ticket buy side, reusing the allocation-bar thresholds (>50% red + ⚠, ≥34% dim). No new prop — `PlayerView` already carries gp/inventory/lastPrice. UI-only, no redeploy. ~~wealth composition by liquidity (cash / buy-offers / goods + at-risk loot)~~ DONE 12m (`worthBreakdown`/`WealthPanel`). ~~"average down" helper (blended cost preview when adding to a position)~~ DONE 12d (`blendBuy`); ~~break-even sell hint (floor-tax-exact sell floor + below-break-even warning)~~ DONE 12h (`breakEvenSell`). The ticket's pre-trade instrumentation is now symmetric (buy: how it reshapes cost basis ↔ sell: the price that clears it). ~~return on starting stake (am-I-up headline)~~ DONE 13k: `returnOnStake(startGp, worth)` (game.ts, pure) + a ↑/↓ gp+% badge in the WealthPanel header (green/red). Honest scoping: lifetime vs the original stake (startGp persists across reloads), NOT session-scoped. UI-only, no redeploy. ~~an **affordability lens** on TopFlips ("flips I can do now" given gp)~~ DONE 13l: `flipAffordability(buy, gp, limit)` (pure, in TopFlips.tsx) → per-row ×N badge (✕ + dim when unaffordable) + a "fits purse" filter that surfaces the best affordable flip. Honest scoping: a capital/limit cap (units you can OPEN), NOT a realizable-profit claim (depth is unknowable — that ranking stays declined). 13t: a "GE" tag now marks flips you can afford but the GE buy limit caps (gp to spare) — the "more capital won't help here" signal; `limited` flag tightened (zero-limit ≠ capped) per an adversarial review that found the 4 new money helpers otherwise SOUND. UI-only, no redeploy. Next idea: surface the scorecard/paper-P&L in the masthead; ~~a sort toggle on Open Positions if the held list gets long~~ DONE 18s: a P&L/value/band sort chip row on PositionsPanel — since the panel caps at 8 rows, the sort decides WHICH surface (value=biggest exposure, band=ripest to offload, beyond the P&L default). Also a visual QA pass (screenshot e2e + image read): Exchange tab healthy, no regression. UI-only, no redeploy. ~~the Hall tab packed 6 panels in column 1 and 1 each in cols 2-3 (6/1/1) → a large desktop void~~ DONE 18t: visual QA extended to Adventure (healthy) + Hall (found the void); redistributed the Hall panels across its 3 columns to 3/3/2 (identity/progression/competition), filling the void — pure JSX reorder (class-based locators unaffected, every panel renders), verified by a second screenshot. UI-only, no redeploy.; ~~tint the sell price input green/red around the break-even line~~ DONE 13q (`input.belowbe` reds the price field when a sell is below `breakEvenSell` — loss-prevention at the input); ~~**highlight underwater positions** in PositionsPanel~~ DONE 13n: `underwaterSummary` (pure) + a "⚠ N underwater −X paper" callout + a red left-edge on losing rows (they sort to the bottom, so the aggregate + tint surface them without scrolling). UI-only, no redeploy. Remaining: ~~a per-flip **round-trip trade journal**~~ DONE 13u: `recentFlips(fills, tax)` (pure FIFO-match, keeps per-flip detail the tradeBook aggregates away) + a "flips" mode on TradeFeed (tape/mine/flips). Window-scoped (FILLS_CAP=50); full history would need a persisted log. UI-only, no redeploy. Remaining: ~~a session-scoped (this-load) P&L via a captured baseline (distinct from 13k's lifetime figure)~~ DONE 14r: `sessionPnL(worth, baseline)` (pure) + a "this session ↑/↓ X (±Y%)" line in the WealthPanel; the baseline lives in App (always-mounted, so it survives tab switches) keyed to game identity (resets on a swap) — the unmount-sibling of the 14e/14f swap-guard class. UI-only, no redeploy. ~~A "cut losers" one-click sell on an underwater row~~ DONE 14s: a two-tap (arm→confirm) "✂ cut" chip on underwater PositionsPanel rows that market-sells the whole position at bestBid via the existing `place` command (gated on a new optional `onCommand` prop; `stopPropagation` so it doesn't also load the row). UI-only, no redeploy. ~~Its profit-side twin~~ DONE 21d: a "✓ take" chip on WINNING rows (same two-tap arm/confirm, shares `armed`), gated on `bestBid > breakEvenSell(avgCost, tax)` so it only offers to lock a REAL after-tax gain (not a paper one the spread/tax would eat); green vs cut's red. Loss-cutting + profit-taking are now symmetric. 2 tests. UI-only, no redeploy. **FIXED 21k (adversarial review):** the shared `armed` state was item-keyed, so a cut armed while underwater could silently become a take confirm if the row flipped green between renders — now keyed by item+ACTION (`id|cut`/`id|take`); regression test added. (Same review verified staleOffers/affordEta/handle-lift/eventMove/diveSurvival/deeds/a11y SOUND.) ~~sortable flip-margin column on the market table (full-market lens vs TopFlips' top-4)~~ DONE 14d: `flipMargin` per row + a sortable "margin" column (reuses rankFlips' math; mobile-verified). ~~realized recent volatility readout (live counterpart to the static volatility tier)~~ DONE 14p: `priceSwing(recentPrices)` (pure) + a "recent lo–hi · swing X% · 🟢 steady/🟡 choppy/🔴 wild" line under the ticket sparkline — flags when a spread is too choppy to trust before both legs fill; reuses the sparkline's trade window, UI-only. ~~realized-swing as a sortable MarketTable column~~ DONE 16y: a sortable "swing %" column (peak-to-trough over the sparkline window via `priceSwing`, computed once into a `swings` map shared by the comparator + cell; steady→green, wild→red, choppy→plain; untraded → '—' sinks). The third sortable-decision column (margin=profit · band=value · swing=risk). UI-only, no redeploy. ~~a "steady" market track (like "cheap"/"flippable")~~ DONE 17h: a `steady` track (predicate `swings.get(id)?.read === 'steady'`, reusing the 16y swings map) — only calm AND liquid markets (untraded = no read = excluded). Completes the volatility tooling: ticket read (14p) → sortable column (16y) → filter track (17h). UI-only, no redeploy. Next idea: std-dev/OHLC for power users. ~~a momentum (vs-EMA) sortable column — the direction axis~~ DONE 17o: a `mom` column (`(last−ema)/ema` %, coloured up/down, sortable; no-EMA sinks) — completes the decision-axis set: margin=profit · band=value · swing=risk · mom=direction. Most useful during events (the economy otherwise anchors near EMA). UI-only, no redeploy. ~~a "movers" filter track (one-click "what's moving")~~ DONE 17z: a `movers` track (predicate `|momentum| ≥ 0.05`, where `momentum(last, ema)` is now a pure game.ts helper — extracted as the third caller after the column + its sort) surfacing items dislocated ≥5% from their EMA; empty in the calm baseline, lights up on events. Completes the momentum tooling (column 17o → track 17z), matching the volatility axis (read 14p → column 16y → track 17h). UI-only, no redeploy. ~~value-band market filter (cheap/fair/rich position in the cost→value band)~~ DONE 15f: `valueBand(def, lastPrice)` (pure — extracted from the ticket's inline cheap/fair/rich, now its single source) + a "cheap" MarketTable track surfacing accumulation candidates (orthogonal to flippable/movers). UI-only, no redeploy. ~~value-band on Open Positions (sell signal on holdings)~~ DONE 15o: a 🟢/⚪/🟡 `valueBand` tag beside each position's mark — rich = ripe to offload, cheap = room to run; reuses `valueBand` (5th surface). UI-only, no redeploy. ~~a "rich" market track (offload candidates); value-band as a sortable column~~ DONE 15q: extracted `bandPosition(def, last)` (the clamped 0..1 fraction `valueBand` already computed internally; `valueBand` now categorizes it — thresholds in one place) + a sortable "band" 🟢/⚪/🟡 column on the MarketTable (sort ↑ for accumulation candidates, ↓ for offload — supersedes a one-sided rich track). The refactor is behaviour-preserving by construction (the 5 `valueBand` callers can't regress). UI-only, no redeploy. (Ruled out "fills while away" first: the engine's 512-trade window evicts away-fills before `recordFills` — needs an engine-side accumulator, Jesse-gated.) ~~band METER — visualize the concrete cost→value range (floor/ceiling + position), not just the cheap/fair/rich third~~ DONE 16i: `BandMeter({def, lastPrice})` (reuses `bandPosition`/`valueBand`) — a floor─[marker]─ceiling bar on the ticket with a green/gray/gold zone track, surfacing the band's actual bounds + exact position (the magnitude the category hid). UI-only, no redeploy. Next (band): the meter on positions/watchlist rows if it reads well. ~~market mood / breadth read (macro lens)~~ DONE 15g: `marketMood(markets, items)` (pure) → up/down (traded only, last-vs-EMA) + cheap/rich (via `valueBand`) counts, shown as a "📊 N↑ / M↓ · 🟢 cheap · 🟡 rich" line above the market table. UI-only, no redeploy. **Trading is deep — keep pivoting across systems (RPG content / progression / robustness / retention) for breadth between trading bricks.** ~~the market table grew dense (10 columns) — a declutter option~~ DONE 17r: a `compact` toggle (`usePref('ew-market-compact')`) adds a `.market.compact` class; CSS hides the 5 analysis columns (mom/margin/band/swing/trend via nth-child 5–9), keeping item/bid/ask/last/volume. Unit asserts the class+persist; an e2e verifies the column-index correctness in a real browser (compact → mom/margin hidden, bid/volume shown). The honest response to the #278 density finding — give the player the choice. UI-only, no redeploy. ~~a placed offer that RESTS (no instant fill — the common maker flip) showed no ticket confirmation (only reject + "filled instantly" were handled)~~ DONE 18h: `lastResult` is now tagged with its command (`{result, cmd}` — contained reshape, TradeTicket is its only consumer) so a resting place (`ok && trades===0 && cmd.type==='place'`) shows "✓ placed — resting on the book" without false-firing on a cancel/claim (which also have empty trades). Asserted on the resting-buy + abort tests. UI-only, no redeploy. ~~a resting offer gave no sense of staleness (just remaining @ price) — a flipper can't spot dead capital~~ DONE 18i: `orderAge(world, order)` (pure — reads the placement `Order.tick` from `world.books`, which the view projection drops but the UI may read for display like `bidWalk`; null when off-book) + a "· rested {N}t" readout on each Open Offer + a ⏳ `.stale` flag past `STALE_ORDER_TICKS=500` (re-price/abort dead capital). No engine/view change. Unit + render test. UI-only, no redeploy. ~~the per-offer ⏳ stale flag had no AGGREGATE~~ DONE 21e: a pure `staleOffers(world, openOrders)` (count + gp idle in stale buys + the fully-stale itemId+side pairs safe to bulk-cancel) drives an Open-Offers header line "⏳ N stale · ≈X gp idle" (the offers mirror of the underwater summary 13n) + a one-click "abort stale" that cancels only pairs where EVERY offer is stale (never a fresh one sharing the pair). Cancel-only (bulk reprice is unsafe — sequential gp/limit depletion strands re-places). 4 tests. UI-only, no redeploy. ~~the session's new feedback classes (`.resting` 18h, `li.stale`/`span.stale` 18i) shipped without CSS — only the emoji carried the signal, and `.resting` sat unstyled beside its styled siblings `.reject`/`.filled`~~ DONE 18k: styled them with existing tokens (`.resting` → `--gold`, matching the `.reject`/`.filled` family; `li.stale`/`span.stale` → amber accent). A real-browser e2e (`toHaveCSS`, the 17r precedent) confirms `.resting` is gold rgb(212,169,55) — proving the stylesheet loads + applies. UI-only, no redeploy.
- **Retention**: ~~celebrate keeping the daily streak (the reward half, paired with the existing FOMO nudge)~~ DONE 15m: `streakCelebration(prev, next)` (pure — continuation ≥2, new-best flagged) + a "🔥 N-day daily streak!" toast in App's streak effect. UI-only, no redeploy.
- **Events**: ~~make the active-event strip actionable~~ DONE 15l (each `⚡ {item} {kind} · N left` chip is now a button that `setSelected` + `pickRoom('exchange')` to trade the event item — the Storm-Trader play, one click — with the item's real icon). UI-only, no redeploy. ~~event-END recap (close the lifecycle the chip opens)~~ DONE 15z: `reconcileEvents(captured, activeNow, priceOf)` (pure set-diff — capture each event's price when it goes active, recap the price move when it ends) + a "⚡ {item} {label} ended · settled {start}→{end} (±X%)" toast in `refreshProgress` (swap-guarded like 14e/14f). The event lifecycle (start chip → end recap) now mirrors the dive lifecycle (15n). UI-only, no redeploy. ~~an "ending soon" urgency cue on active-event chips (the closing-window nudge)~~ DONE 17e: `eventEndingSoon(ticksLeft)` (pure, ≤150 ticks = the final stretch; absolute so "≈150 ticks to act" reads consistently across 800–2000-tick events) + an `.ending` amber+⏳ chip state. Completes the event lifecycle cue (begin chip 15l → ending 17e → end recap 15z). UI-only, no redeploy. ~~the chip showed kind + ticks left but never the live price-move MAGNITUDE (a 2% wiggle vs a 15% craze looked identical)~~ DONE 21i: a pure `eventMove(startPrice, currentEma)` (the recap's `(now−start)/start` math, EMA basis) drives a "· ±X%" on the chip, reusing the `seenEvents.startPrice` updateNews already captures. Factual only — no buy/sell direction (engine-assumption). null until capture lands → graceful. 3 tests. ~~the move was only on the chip, not at the trade DECISION point~~ DONE 21l: the same `eventMove` now feeds the ticket's `eventNote` ("· ±X%"), suppressed at 0% (also cleaned the chip's brief "+0%" at start). Verified the full market-sanity gate after 13 UI bricks: 20 files/659 tests green, sim invariants OK on seeds 11/42/1337, seed-42 hash fe75df57 unchanged (engine determinism untouched). Next (events): scope the recap to held/watched items if it proves noisy. ("events that ended while away" is ENGINE-GATED — sim.ts:143 prunes ended events, ruled out 17a-design.)
- **Robustness**: ~~UI to restore/export a quarantined save on next boot~~ DONE 10u (boot recovery bar: download/discard/dismiss). ~~warn before the manual new-game "start" wipes a run with progress~~ DONE 14x: `restartStakes(game, worth)` (pure) + a gated "⚠ wipes this run — {worth} net · {N} deeds" note in the seed form (shown only when deeds>0 or worth>startGp — no alarm on a fresh run); matches the challenge path's existing warning. UI-only, no redeploy. Remaining: **stale-monster-id crash vector (found 15t)** — `monsterById(id)` THROWS on an unknown id (`quest.ts:374`); persisted state (`exp.combat.monsterId`, a bounty's `monsterId`) could carry an id a later engine version removed/renamed, and the engine TICK calls `monsterById` during combat resolution too, so a UI render-guard alone is insufficient. Proper fix = load-boundary quarantine (settle/drop a stale persisted combat in `normalizeGame`), which touches engine semantics → **Jesse-gated**. Realized probability ~nil today (content has been additive), but a real latent crash as the bestiary evolves. Also: guard itemDef at UI read sites (most use `?? id` already); 100k-tick determinism gate (NOTE: `longrun.test.ts` already does a 50k snapshot-resume hash gate — a 100k version is marginal CI time). **Save-flow review (16j) — DEFERRED items:** (a) cross-device double-accrual of offline time (review #2) — DISPLAY-only worth inflation (the verified leaderboard score replays from `commandLog`, idle offline ticks reproduce identically), needs a multi-device-model decision (Jesse) before `pushCloudSave` restamps `lastSeenMs`; (b) `recordFills` misses fills evicted from the 512-trade window across a long catch-up (by-design incompleteness, not corruption). FIXED in 16j: catch-up swap-guard (HIGH) + `newGame`/`normalizeGame` `delves` parity. ~~the AccountBar's "✓ synced" badge only updated on a SUCCESSFUL push, so a FAILED cloud push left a stale "✓ synced" (false reassurance — the player's latest isn't backed up)~~ DONE 19d: added a `syncFailed` flag (set on the push result in `schedulePush`) → the badge shows "⚠ unsynced" on failure, clearing on the next success. First AccountBar test. UI-only (client feedback; backend gated), no redeploy. **Engine core review (16m) — verdict SOUND** (conservation ledger complete incl. pack/worn item paths; determinism holds — single rng cursor, zero-draw bounty/expedition streams, no unsorted-Record hashed decision; matching price-time-correct, self-trade-skip, `TRADE_WINDOW` display-only). One MEDIUM fragility FIXED: `deriveStats` equal-score pack tie-break now resolves by `itemId <` (uniform with `equipBest`/`worn`) — byte-identical (sim hash `fe75df57` unchanged), proactively closing a latent determinism trap.
- **RPG content**: ~~Field Forge event~~ SHIPPED 11e; ~~Skarn elite (wilderness_ruins)~~ SHIPPED 11g; ~~Blood Altar event (hp→atk, forge's inverse)~~ SHIPPED 11j — **all merged to main in 12b (PR #1); verify-score redeploy PENDING (see header)**. more event faces (each bends a still-untouched system — FINDINGS #64); a 9th region (needs a NEW resource to extort); ~~attack brew~~ DONE 10n (goading → +10 atk/dive, the offensive counterpart to bastion); encounter depletion (only if an audit shows the TAS tail needs bounding). Idea: named elites for the other shallow regions (0–3); a defensive brew/altar (gp/hp → def) to round out the boost matrix.
- **Side-boards**: ~~contract premium % + ready flag~~ 11y; ~~bounty progress bar + reward-per-kill~~ 11z. Both decision-instrumented now. ~~a bounty needs to say WHERE to hunt the target~~ DONE 17x: `monsterRegions(id)` (pure inverse of `regionRoster` — the regions a monster appears in via pool/elite, shallowest first) + a "· {region}" suffix on each bounty target, so a kill order carries its location. UI-only, no redeploy. ~~a not-done bounty names WHERE but offers no way to GO (passive "hunting…")~~ DONE 18f: `huntRegionId(monsterId)` (the id-returning sibling of `monsterRegions` — shallowest region id, null if nowhere) + a "hunt" button on not-done bounties that jumps to the Adventure tab with the target's region pre-selected (App passes `onHunt={jumpToRegion}`, the Delve-Log "raid again" 12l pulse's second caller); done → "claim", no-onHunt → "hunting…". The bounty sibling of the contract "buy {N}" (17y). UI-only, no redeploy. ~~a not-ready contract names goods but offers no way to acquire them (the disabled "deliver" is a dead end)~~ DONE 17y: `ContractsBoard` gains an optional `onSelect(itemId)` — a not-ready contract now shows a "buy {qty−have}" chip that loads the item into the ticket (App wires `setSelected(id); pickRoom('exchange')`), the contract sibling of the event-chip jump (15l) and gear best-buy (14z). Ready → "deliver" unchanged; no-onSelect callers keep the disabled fallback. Closes see→acquire→deliver. UI-only, no redeploy.
- **Alerts**: ~~buy-below~~ 10b; ~~sell-above (take-profit)~~ 12a (`alertHit`, symmetric); ~~resting-fill notification (toast when your resting order fills during live ticks, 1×-gated)~~ DONE 13b (`fillSummary`, `recordFills` returns fresh fills). ~~value-band "buy the dip" alert (fires when a watched item enters its cheap band — threshold-free, self-adjusting)~~ DONE 15s: `bandAlertHit(def, last)` (= `valueBand === 'cheap'`) + a `bandAlerts` pref + a 🟢 toggle on banded watch rows; one-shot toast on entering cheap, re-arms on leaving, copying the price-alert pattern (only the predicate differs). UI-only, no redeploy. The price-alert + fill-feedback system is complete (enter, exit, fill) and now has a fundamentals-relative trigger too. ~~a "rich/offload" band alert (sell-side)~~ DONE 16d: `richAlertHit(def, last)` (= `valueBand === 'rich'`) + a `richAlerts` pref + a 🟡 toggle beside the 🟢 cheap toggle on banded watch rows; one-shot "🟡 {item} is rich — take profit?" toast, re-arms on leaving rich. The band-alert pair (cheap/rich) is complete and lives on one surface. The alert system is now fully symmetric: absolute (≤/≥) AND fundamental (🟢/🟡), entry and exit on both axes. UI-only, no redeploy. **Toast priority (fixed 16e, adversarial-review-driven):** `refreshProgress` is last-writer-wins, so celebrations (level-up/region-unlock/deed) now fire LAST to WIN the slot over frequent informational alerts/event-recaps (they had been appended-after and silently outranking the celebrations); the four `*Fired` Sets also now clear on a game swap (informational alerts fire-fresh on adopted state, unlike celebrations which suppress). FINDINGS #243.
- **Progression**: ~~progress bars on the closest-unearned deeds (was bare % text)~~ DONE 12z (`.deedbar`, MilestonesPanel) — progression surfaces now visually consistent with the bounty/conquest/allocation bars. ~~an ETA ("≈2h to gp Millionaire") on worth deeds~~ DONE 17k: `deedEta(progress, worth, perMin)` (pure — recovers the threshold from the raw progress, returns tick-minutes; null on flat/falling/done) + a `Milestone.paceMetric?: 'worth'` tag on the 5 worth deeds so the gp/min `worthRate` ETA only applies where the units match. Shown beside the deed bar (same `fmtDuration(eta*60)` as the WorthChart's next-round-number ETA 11w). UI-only, no redeploy. ~~a player-SET custom worth goal (agency beyond the fixed deeds)~~ DONE 17l: `goalView(goal, worth, perMin)` (pure — pct capped 100, remaining, etaMin, reached) + a settable/clearable target in the WealthPanel (`usePref('ew-worth-goal')`), with the same progress+ETA treatment. The third consumer of the worth-rate projection (11w round-number, 17k deeds, 17l custom). UI-only, no redeploy. ~~a one-shot "🎯 target reached!" toast~~ DONE 17m: a WealthPanel effect fires "🎯 Goal reached!" once when worth crosses the target, guarded by a persisted `goalHit` (the goal value already celebrated — fires once per goal, survives reload, like the duel's delete+save 16v); `commitGoal` marks an already-met goal as hit (no hollow instant toast). Fires from the child effect so an offline crossing is caught for free. The goal loop is complete: set → track → celebrate. UI-only, no redeploy. ~~the goal as a target line on the Fortune chart~~ DONE 17t: `WorthChart` reads the goal live from localStorage (no state-lift of 17l/m/p) and, when chartable (`goal ≤ dataMax*3` so it doesn't flatten the curve), folds it into the y-range + draws a teal dashed `.goalline` + a 🎯 note — the climb made spatial. The goal now lives on three surfaces: number+ETA (WealthPanel 17l), the moment (toast 17m), the climb (chart 17t). UI-only, no redeploy. 10i region-mastery xp; 10k sellsword haul counters; 10l offline sellsword summary in the away-bar; ~~lifetime records profile~~ DONE 10z (RecordsPanel in the Hall); ~~peak-dive records (biggest single haul, most cleared in a dive)~~ DONE 15d (`diveRecords(delves)` — best banked haul counts only survived dives; most-kills counts any — + two region-named rows in RecordsPanel; UI-only). ~~dive survival streak (consecutive clean extractions — risk-management retention hook)~~ DONE 16f: `diveStreak(delves)` (pure single-pass → `{current, best}`; a death resets the trailing run but not the best) + a "🔥 {current} clean (best {best})" line in the Delve Log header. The adventure analog of the daily streak; adds stakes to the bank/push call. UI-only, no redeploy. ~~a survival-streak celebration toast~~ DONE 16g: `isStreakMilestone(current)` (exact membership in {5,10,25,50,100}) + a "🔥 {N}-dive survival streak!" toast in `onDelveEnd`. MILESTONE-gated, not every-new-best (a +1 counter would toast every dive past your best — noisy); exact-equality on the monotonic counter is a once-each trigger with no dedup state. Readout (16f) + celebration (16g) complete the streak feature. UI-only, no redeploy. ~~a permanent DEED for the survival streak (the careful-diver counterpart to nine-lives, which rewards 9 deaths)~~ DONE 21f: an "Untouchable" milestone (`diveStreak(g.delves).best >= 10`, progress `best/10`) reusing the streak helper so deed/readout/toast agree. Deeds are UI-side + replay-inert (checkMilestones never runs in replayRun), so no engine change / no redeploy / no save migration. 1 test. ~~the streak + deaths were shown but never the RATE~~ DONE 21j: a pure `diveSurvival(delves)` → {total, survived, survivalPct, avgHaul} + an Almanac "dives survived: X/N (P%) · avg Y gp" row (avgHaul over survived dives only, the raidTotals banked mapping). The honest self-assessment behind the streak; first AlmanacPanel test added. UI-only, no redeploy. ~~a "new best haul!" celebration toast~~ DONE 15e: `isNewBestHaul(priorDelves, record)` (pure) + a "🏆 New best haul!" toast fired in `onDelveEnd` when a survived dive beats the prior best (detected at the append point — no baseline ref / swap-guard needed, since onDelveEnd only fires on a real dive-end; first dive sets the bar silently). UI-only, no redeploy. Next: ~~timestamped deeds (🏅 latest · Nm ago)~~ DONE 11s; ~~deed markers on the Fortune chart~~ DONE 11t (`WorthChart` milestones prop). ~~gp sink that aids raiding~~ SHIPPED 11e (Field Forge: burn 300gp → +8 atk this delve); ~~per-region monster-mastery completion codex (👑 when a region's whole roster falls)~~ DONE 12f (`regionMastery`/`ConquestPanel`, Hall); ~~persistent Delve Log — per-expedition history (region/kills/loot/survived-died), latched at end~~ DONE 12i (`summarizeDelve`/`DelvePanel`); ~~click a Delve Log row to re-raid that region (jump to Adventure tab)~~ DONE 12l (regionPick nonce pulse); ~~raid risk/reward totals (loot banked vs lost) in the Delve Log header~~ DONE 12s (`raidTotals`). ~~per-region raid risk/reward breakdown (which region is the best/deadliest farm)~~ DONE 15c: `raidTotalsByRegion(delves)` (pure, ranked best-net first) + a "by region — best farm first" block in the Delve Log, shown only at ≥2 regions raided. UI-only, no redeploy. Next (raid analytics, DEFERRED — needs engine): per-dive item loot summary (`ExpeditionState` merges drops into `pack` with no per-dive loot-items accumulator — a replay-affecting field). More: ~~flash the picked region on the RegionMap~~ DONE 14t (a 600ms `@keyframes mapflash` pulse on the selected node when `selected` changes — e.g. a Delve Log "raid again" jump; skip-mount so the static start doesn't strobe; UI-only); ~~a "realm conquered" deed when all 8 regions hit 👑~~ DONE 14q (`realm-conquered` milestone, reuses `regionMastery` for the done-check + an X/8 progress bar; UI-only); ~~per-region roster glyphs (lit/dim) for which-foe-is-left~~ DONE 14n (ConquestPanel rows now show each region's roster as lit/dim `MonsterGlyph`s — dim = the foes left to hunt; reuses `regionRoster` + `killsByMonster`, UI-only); final-region (Abyss) mastery hook. ~~a "new region unlocked!" celebration when the frontier advances~~ DONE 15h: a "🗺 New frontier unlocked!" toast fired in `refreshProgress` when `questProgress` climbs (level-up-style baseline, same game-swap guard; detection stays in refreshProgress because questProgress updates synchronously in the command — the mirror of 15e). UI-only, no redeploy. ~~an elite first-kill celebration (salute the first time each named boss falls)~~ DONE 16w: `newElites(slain, kills)` (pure — the elites with a kill not yet in the baseline Set) + an `eliteFired` Set guarded by an `eliteBaselineGame` swap-ref, baselined on boot/swap to the elites already in `killsByMonster` (a prior-session/offline kill adopted silently, like the level baseline) + a "☠ {elite} falls!" toast in `refreshProgress`, ordered below level/region but below a coincident deed/duel (both rarer/meta). 4 `newElites` units + a `+1k`-driven render test (boot kill silent, live kill fires). UI-only, no redeploy. ~~the elite capstone — a deed + a collected hunt tracker for all four named elites~~ DONE 16z: an `apex-predator` MILESTONE (`ELITES.every(e => killsByMonster[e.id] > 0)` + progress, deeper than elder-slayer's "any one elite") + a gold-bordered "☠ Named Elites N/4" strip in the ConquestPanel collecting the four bosses (scattered one-per-deep-region) with lit/dim glyphs, both off the shared `ELITES` roster. `.elitehunt` (not `.conquest-row`, so region-row tests hold). 2 tests (deed + tracker). UI-only, no redeploy. The 16w salute → 16z standing goal completes the elite arc. Next (optional): a Bestiary flourish on the elite row; a deeper "first elite EVER" vs per-elite distinction. **New deeds (UI-side, replay-inert — the safe content lever while engine content is Jesse-gated):** ~~Untouchable~~ DONE 21f (`diveStreak.best >= 10` — careful-diver counterpart to nine-lives); ~~Profiteer~~ DONE 21g (`totalRealized(tradeBook) >= 100k` — the core flip loop's SUCCESS, which the worth tiers measure as net worth, idle-reachable, not trading profit). Future deed ideas: a 1M-realized capstone; a "diversification" deed (gameable — needs care); a deeper survival rung (25/50).
- **Social/retention** (daily loop CLOSED — ember + nudge share one "day" definition): ~~shareable run brag (the viral loop: "what I did" + a challenge link)~~ DONE 16k: `bragText(game, worth, origin, pathname)` (pure — combat level · worth · best haul · streak · deeds, omitting zeros, + a `#seed=S` challenge link) + a "📋 brag" chip beside "challenge link", copied via the `copyChallenge` clipboard pattern. UI-only, no redeploy. ~~native share for the brag (mobile share sheet)~~ DONE 16l: `shareBrag` prefers `navigator.share({ text })` (one tap to any app on mobile — the real viral path), falling back to the clipboard copy on desktop/unsupported. Progressive enhancement, durable Web Share API over a per-platform intent URL. UI-only, no redeploy. ~~an image/card render of the brag~~ DONE 16o: `BragCard({game, worth})` — a styled SVG "Run Card" in the Hall (combat level · net worth hero + best-haul/survival/deeds chips + seed/site, stone-and-gold livery), screenshot-able. Dependency-free + fully testable (SVG DOM, not canvas); omits empty chips like `bragText`. UI-only, no redeploy. ~~share-as-file of the card~~ DONE 16q: the Run Card is now self-contained (styles embedded in the SVG with literal colours/font-fallbacks) with a "💾 save" chip that exports it as an `image/svg+xml` Blob via `navigator.share({files})` (mobile) or download. SVG-not-PNG to stay canvas-free + testable (`shareOrDownload` unit-tested via a `canShare` mock). The social arc is complete: link → text brag → native share → visual card → shareable image file. ~~the SHARED LINK itself had no preview (no OG/Twitter meta) — a bare URL on Discord/Twitter~~ DONE 18z: added `index.html` description + OG + Twitter (`summary_large_image`) meta with a deployed 1200×630 `public/og.png` (the "Exchange Wars" masthead + terminal, captured via the on-demand screenshot e2e), so every shared challenge/brag/duel link previews as a branded card. e2e asserts the meta is served. UI-only, no redeploy. ~~the shared-link CLICK then lands on a blank dark `#root` until the bundle loads + React mounts~~ DONE 19a: a branded, inline-styled loading fallback ("Exchange Wars" + "opening the exchange…") inside `#root` that `createRoot().render()` replaces on mount — the first paint is intentional, not "broken," for the 18z share-link clicks (+ cold/slow loads). Inline styles (CSS bundle isn't applied yet); zero JS. UI-only, no redeploy. ~~the PWA manifest had the essentials but no rich-install metadata (Chrome's install dialog showed a bare entry)~~ DONE 19b: added `screenshots` (reusing the deployed 1200×630 og.png as the app screenshot) + `categories: ["games"]`; an e2e fetches the SERVED manifest + parses it (a JSON typo silently disables install — caught now). Completes the distribution surface: link previews (og 18z) + install preview (manifest). UI-only, no redeploy. ~~the README (the repo's public front door + GitHub link preview) was stale on game content: "seven-region… to the Inferno Gate" + "two named elites"~~ DONE 19c: corrected to eight regions (→ The Abyss) + four named elites (Skarn/Vorkanth/Zukrath/Vessith) + the test count, verified vs quest.ts:151–163. Facts only, prose untouched. Docs-only, no redeploy. Next (social, optional): a PNG render if SVG share proves spotty on a platform (needs canvas — jsdom-untestable). ~~daily shared seed~~ 10q; ~~"today" badge~~ 10r; ~~streak counter~~ 10s; ~~streak-at-risk nudge~~ 10t; ~~"beat my score" challenge/brag links (encode the sharer's worth+handle → a direct duel)~~ DONE 16s: `challengeLink(origin, pathname, seed, worth?, handle?)` → `#seed=N&w=W&by=H` + `parseChallengeTarget` (the CLAIMED "beat me" figure, not a verified score) + a tolerant `parseChallengeSeed`; the challenge bar shows "{handle} dares you to beat {worth} on seed N". The brag's link is now a duel; closes the brag→duel→brag-back loop. UI-only, no redeploy. ~~the duel, RACED (accept → see the target during play → celebrate beating it)~~ DONE 16t: `Game.duelTarget` (UI-only persisted field, like `ghost`) stamped on the accepted run; a "⚔ dueling {handle} — beat {worth} (you: {now})" banner while live; `duelWon(target, worth)` → a one-shot "🏆 You beat {handle}!" in refreshProgress that `delete`s+saves the target (fire-once, survives reload). The duel is now a goal you SEE yourself racing and a moment you WIN. UI-only, no redeploy. ~~the GHOST race (17t) had only a quiet Hall-chart "vs ghost" stat, no always-visible presence like the duel banner~~ DONE 21n: a parallel masthead ghost-race banner ("🏁 racing your best run — beat {ghostNow} (you: {now}, ±X)"), reusing the exported `ghostWorthAt`, gated on `ghost.seed===seed && !duelTarget` (the duel takes precedence — no double banner). Completes the duel/ghost symmetry. 2 tests. ~~"you" highlight + rank on the Sprint Board~~ 11q (`myRank`); ~~rank-gap climb target (gp to overtake the rank above)~~ DONE 16a: `rankGap(rows, meRank)` (pure — worth gap + handle of the rank directly above; null at #1/unranked) + a "🎯 {gap} gp behind #N {handle}" board line. Turns the standings into a target. UI-only, no redeploy. ~~a "you'd rank #N" projection of your CURRENT (pre-sprint-mark) worth~~ DONE 16b: `provisionalRank(rows, myWorth)` (pure, `>=` so a tie sits below the incumbent) + a "⏱ if the sprint ended now (tick X/2000), your {worth} gp would sit #N" board line, gated to `tick < SPRINT_TICKS` (honest only inside the sprint window). 16a (submitted gap) + 16b (live provisional) = a two-tense board (where you are / where you're headed). UI-only, no redeploy. ~~a gap-to-#1 for top contenders~~ DONE 18a: `gapToTop(rows, meRank)` (mirrors `rankGap`'s shape/guards — gap to `rows[0]` + leader handle, tie-floored at 0) + a "👑 {gap} gp behind #1 {leader}" board line gated to ranks #3–#10 (at #2 it duplicates the rank-above gap; below #10 the leader isn't a live target). Two targets now: the next rung (rankGap) + the summit (gapToTop). UI-only, no redeploy. ~~"Daily Board" framing when seed == dailySeed()~~ 11r. ~~per-day personal best ("🏁 daily best")~~ DONE 12e (`recordDailyBest`/`dailyBestView` — masthead 🏁 tag, ▲ at a new peak, persisted); ~~"🎉 new daily record!" celebration toast on beating your prior best~~ DONE 12w (`beatRecord`). The daily↔leaderboard loop is now closed and the solo high-score hook is in (passive tag + active celebration). Leftover daily polish (low priority): a one-time "🎉 new record!" toast vs the always-on ▲; "today's board" framing on the leaderboard view. ~~the handle (`ew-handle`) feeding brag/duel/Run Card had NO offline setter — the only one was the cloud+auth-gated LeaderboardPanel submit row, so signed-out / no-backend players were stuck anonymous~~ DONE 21b: App now owns the handle (single source of truth, raw-persisted) with an always-available `HandleField` in the Hall; brag/challenge/BragCard read it live; LeaderboardPanel takes it as an optional controlled prop. Possible follow-up: also surface the handle field by the masthead brag/challenge chips if Hall discoverability proves low.
- **Input/UX**: ~~tab activity badges (a dot when an off-tab event fires — fills/alerts on Exchange, a deed on Hall — cleared on visit)~~ DONE 16r: `markRooms(prev, fired, active)` (pure — flag a room iff its event fired off-tab and isn't already flagged; same-ref when unchanged so the per-tick caller bails the re-render) + a `roomRef`/`unseen` state in App, the `firedExchange`/`firedHall` flags set where the toasts fire, and a `.tabdot`. The persistent complement to the transient toast. UI-only, no redeploy. ~~keyboard shortcuts (1/2/3 rooms, p pause, ? help)~~ DONE 11a; ~~market-row nav (j/k or ↑/↓), matching the displayed sort/filter~~ DONE 12g (`nextRowIndex`, MarketTable `active`-gated window listener); ~~ticket side hotkeys (b/s pick buy/sell)~~ DONE 12t. The trade loop is now keyboard-driven end to end (j/k pick item → b/s pick side → type → Enter submits); ~~scroll the keyboard-selected row into view on long lists~~ DONE 12v (`scrollIntoView` block:nearest + typeof guard). ~~keyboard-drive the adventure embark too~~ DONE 14w: EmbarkPanel takes an `active` prop + a window keydown listener — ←/→ move the selected region (clamped to unlocked), Enter embarks via a shared `doEmbark()`; gated to the active Adventure tab and guarded against typing. UI-only, no redeploy. Next: quick-select digits for top market rows (mind the 1/2/3 room-key collision). ~~combat was the last mouse-only screen (keyboard-playable everywhere else)~~ DONE 18q: `f` swings a combat round (ExpeditionPanel window keydown, gated `active(Adventure) && exp.combat && !typing`, refs-fresh; mutually exclusive with the embark listener which gates on `!exp`). Completes keyboard-playable adventure. Flee/eat stay deliberate clicks (destructive/ambiguous as single keys). Help updated; 2 render tests. UI-only, no redeploy. ~~flee was still mouse-only — combat was only half keyboard-playable~~ DONE 18u: `r`=flee added to the same gated listener (one more branch, `fleeCombat`), completing the combat verb pair (f swings · r flees). Eat stays a click (multiple foods → mis-press wastes a brew). Help + test extended. UI-only, no redeploy. ~~a11y audit: symbol-only buttons lacking an accessible name~~ DONE 18v: swept the UI — the convention holds (every ×/band-alert/watchstar/✕ carries a `title`/`aria-label` = its accessible name) EXCEPT the EmbarkPanel pack steppers (`−`/`+`, no name); added `aria-label="pack one more/fewer {item}"`. **Deferred (logged):** clickable market/positions/watchlist ROWS aren't Tab-focusable (the j/k custom nav covers the keyboard path) — a broader a11y pass. ~~**Also (flagged 20e):** the MarketTable's sortable `<th onClick>` column headers expose `aria-sort` (20e) but aren't keyboard-OPERABLE (WCAG 2.1.1)~~ DONE 20f: each header got `tabIndex={0}` + an Enter/Space `onKeyDown` (via a `sortableProps(key)` helper that also DRY'd the 9 aria-sort/onClick pairs) + a `:focus-visible` ring; kept as a `<th>` (role=columnheader) rather than a child `<button>` (which would break the click tests + need a CSS re-skin). Unit test (Enter+Space+tabIndex) + a real-Chromium e2e (focus → Enter → sort). Closes the sortable-header arc (state 20e + operability 20f). The j/k row-nav and Esc/`/` filter keys are unaffected. UI-only, no redeploy. ~~`w` to star the loaded item to the watchlist~~ DONE 17c: the watch verb of the keyboard trade loop (j/k select → b/s side → Enter submit → w watch) — handled in the MarketTable keydown (active-gated, typing-guarded, navRef-fresh) calling a toasting `onToggleWatch` from App ("★ watching {item}" / "☆ unwatched {item}"). 2 tests. UI-only, no redeploy. ~~a per-row ★ indicator in the MarketTable so the watch state is visible without loading the ticket~~ DONE 17d: a clickable `.watchstar` per row (gold ★ when watched, hollow ☆ brightening on hover; `stopPropagation` so it doesn't also load the row), driven by a `watched` Set, reusing 17c's `onToggleWatch`. The market now shows your watchlist at a glance + gives `w` persistent per-row feedback. UI-only, no redeploy. ~~a "watched" market FILTER track (like cheap/flippable) — one click to see only your starred items~~ DONE 17f: a `watched` track (predicate `watched?.has(id)` + a chip) reusing the `watched` Set from 17d; also refreshed the WatchlistPanel empty hint to name all three add-paths (row ★ / ticket star / `w`). The watchlist arc is complete: add (`w` 17c) → see (★ 17d) → focus (track 17f). UI-only, no redeploy. ~~`/` to focus the market filter (fast item search)~~ DONE 17n: the web-wide "focus search" key, handled in the MarketTable keydown (active-gated, typing-guarded, navRef-fresh) focusing a `filterRef`. The keyboard trade loop is complete: `/` find → j/k walk → b/s side → Enter submit → w watch. UI-only, no redeploy. ~~clear the filter (Esc + ✕)~~ DONE 17s: `Esc` in the filter blanks+blurs it (handled on the input's onKeyDown, since the window listener exempts INPUT targets), and a `✕` button (shown only when non-empty) clears + refocuses via the 17n `filterRef`. Completes the filter UX. UI-only, no redeploy. ~~Escape closes the help overlay~~ DONE 17u: a one-line `Escape → closeHelp()` branch in the App keydown (after the input-guard, so a focused input's Escape — the 17s filter clear — still wins); idempotent `closeHelp` means no `helpOpen` read. Escape is now context-correct: clear the field you're in, else dismiss the modal. UI-only, no redeploy. ~~keyboard speed control~~ DONE 17q: `,`/`.` step the world through the live speeds (1×→5×→20×, clamped) via `resolveShortcut` + a pure `stepSpeed` (paused + `.` resumes at 1×; paused + `,` no-ops); the media/YouTube slower/faster idiom. Core idle-loop pacing is now keyboard-driven (`p` pause · `,`/`.` speed). UI-only, no redeploy.
- **Onboarding**: ~~Getting Started first-steps checklist (auto-hiding)~~ DONE 11v (`firstSteps`); ~~help overlay documents j/k + b/s keyboard shortcuts; start-gp sourced from `HUMAN_START_GP`~~ DONE 13a. ~~deep-link the checklist steps to their tab~~ DONE 15b: each unfinished `firstSteps` entry with a `goto` renders as a `.linkstep` button calling `onGo(room)` (= `pickRoom`) — trade→Exchange, clerk→Hall, delve→Adventure; done/time-gate steps stay plain. UI-only, no redeploy. (Empty-panel hints CONFIRMED present 17h-recon: Watchlist "star an item to track it here", Positions "buy an item to open one".) ~~the market sort columns + filter tracks were undocumented in help; 4 of 8 track chips lacked a tooltip~~ DONE 17j: help's "decide with the numbers" item now names the sort columns (margin/band/swing/volume) + filter tracks (flippable/cheap/steady/gear/watched), and every track chip carries an explanatory `title`. ~~the help drifted behind later features (momentum sort 17o, movers track 17z, compact toggle 17r, Adventure ←/→/Enter embark nav 14w all undocumented)~~ DONE 18d: refreshed the guide — the sort sentence gains momentum, the filter sentence gains movers/exotics/staples + the compact toggle, and the keyboard `<li>` documents the Adventure ←/→ region nav + Enter-embarks (+ Esc clears the filter). Existing help tests preserved (substrings kept) + new /momentum/, /movers/, /pick a region and/ assertions pin it against future drift. UI-only, no redeploy. ~~the 20–21 session tools (handle setter, take-profit/cut, afford-ETA, abort-stale, outgrown nudge) were undocumented~~ DONE 21h: five one-clause additions folded into the existing bullets (no new bullets) + five `toMatch` assertions in the help-content test pin them (the 18d drift-proofing). Notably closed the social-bullet gap — it referenced handles without ever saying you can SET one (21b). UI/docs-only, no redeploy.

> **SATURATION NOTE (17j):** the feature audit is now mostly returning "already shipped" — depth ladder marks your orders (◆), bestiary shows drops, panels have empty-state hints, and the three feature arcs (watchlist add→see→focus / readiness text→map / volatility read→sort→filter) are complete; brick 243 review verdict was SOUND. Remaining autonomous UI value is incremental polish + discoverability. Substantial next steps (new RPG content/mechanics, the parked elites/brews/9th region, the sellsword deaths/deepestRegion deed-scope fix) are **engine-affecting → Jesse-gated on the verify-score redeploy.**
- **Jesse-gated**: prestige/rebirth loop; a deeper art-direction steering pass. **⚠️ ACTION REQUIRED — verify-score redeploy is PENDING for the 12b content merge (Field Forge / Skarn / Blood Altar changed the replay). One command when Jesse is present: `npm run build:fn` (already done — engine.js is current in the repo) then `npx supabase functions deploy verify-score --project-ref chynnshtcjclphlazkmv` (needs `SUPABASE_ACCESS_TOKEN`). Until then, scores submitted with the new content replay against the OLD verifier → a soft worth-discrepancy (NOT a hard reject; the verifier takes only `{handle,seed,log}`). Board is dormant (1 test row) so blast radius ≈ nil. Future engine content: still blocked on the token to redeploy in the same phase.**
  - **Parked engine content (do WITH Jesse — needs review + the redeploy above):** → **now spec'd concretely in `NEXT_CONTENT.md`** (stats/mechanics/files/redeploy checklist). Three batches: (A) named elites for shallow regions 0–3 (Grubfoot/Bone Lord/Obor/Bryophyta); (B) a defensive **altar EVENT** (hp→+def, the Blood Altar's inverse) — NB the defensive **brew** `divine_bastion_potion_4` already ships, so the gap is the event, not a consumable; (C) a 9th region (the Hollow Crown, deepest — spec reuses existing goods to minimise market re-tune). Implement order r2→r3→r1→r0 for the elites (the lumbridge r0 draw is the most seed-test-disruptive). ⚠️ **Adding an elite to a region inserts an `rng.chance(ELITE_CHANCE)` draw in `rollEncounter` — `lumbridge_plains` (region 0) is heavily fixed-seed-tested in `expedition.test.ts`, so expect to re-pin many deterministic assertions (FINDINGS #152). Put new elites in lightly-tested regions first, or budget for the test reconciliation.**

## Decided / dropped
- **Gear durability — DROPPED** (FINDINGS, 9g): raiders already pay spread + death burns + tick cost + consumable tickets; a durability tax re-punishes the validated capital loop for no asked-for benefit.
- **Tier-3 clerk "trades events too" — DROPPED**: clerks farm neither events nor exotics (FINDINGS #27/#35).

## Quality
- **a11y — aria-pressed on toggle controls (20d, brick 316):** several toggles exposed state only via a CSS `active` class — the buy/sell side, market filter tracks, Positions sort, TopFlips fit-only, TradeFeed mode — so a screen reader couldn't tell which was active (WCAG 4.1.2). Added `aria-pressed` (matching the existing convention on compact/watchstar/tracks). A half-applied convention is its own bug; finishing it was mechanical (pressed condition = the active-class condition). 4th a11y dimension in 4 fires (motion → live-regions → modal-focus → toggle-state).
- **a11y — modal focus management for the help dialog (20c, brick 315):** the HelpOverlay (every first run + `?`) had no focus management — focus stayed behind the scrim on open, wasn't restored on close (WCAG 2.4.3). Added `role="dialog" aria-modal aria-label` + focus-in-on-open + restore-on-close (Escape-close already existed 17u). Verified focus VISIBILITY sound in passing (inputs have gold-border focus cues; no global outline suppression). **Follow-up (deferred):** full Tab-TRAP within the dialog (move-in + restore is shipped; the trap is the harder, lower-value remainder). Possible further a11y dimensions: palette contrast (dim/gold on dark), `aria-expanded` on toggle controls.
- **a11y — ARIA live regions for transient feedback (20b, brick 314):** toast (deeds/fills/levelups), trade result (rejected/filled/resting), and the away digest had no live region, so screen readers missed all of it (WCAG 4.1.3). Added `role="status"` (polite) to toast + away digest; wrapped the trade result in a PERSISTENT `role="status" aria-live="polite"` container (a live region must exist before its content changes to announce reliably). Not on the challenge CTA (navigable) or per-slice catch-up progress (would spam). a11y by DIMENSION keeps finding real work: labels/keyboard (18q–v) → motion (20a) → status-messages (20b). **Possible next a11y dimensions:** `focus-visible` rings, colour-contrast of the dim/gold palette, focus management on overlay open/close.
- **a11y — honor prefers-reduced-motion globally (20a, brick 313):** the enumerated `@media` blocks covered only 4 of 29 animations; every LOOPING one (the WCAG 2.3.3 vestibular trigger) had slipped through — incl. the 19s elite-aura pulse I shipped without a guard. Added the standard global `*` block (animation/transition near-zero), drift-proof for future animations; loops are symmetric (settle to rest), splats redundant with the combat log. e2e under `emulateMedia({reducedMotion:'reduce'})` verifies a real looping keyframe collapses to ~0. Lesson: per-item a11y guards rot like per-item validation gates (19x/19y) — use the wildcard. UI/CSS-only, no redeploy.
- **Conservation audit — expedition events (19z, brick 312):** verdict **SOUND**. NB the market-SANITY sim doesn't dive, but `expedition.test.ts` ALREADY covers dive conservation (`checkInvariants` after dives + "choices in the dark" shrine/gamble). Hand-audit ADDED verification of the event kinds those tests don't explicitly trigger — all book through `state.ledger`. **Follow-up CLOSED (investigated 2026-06-13, not needed):** every event kind is already exercised in `expedition.test.ts` (altar×3, forge×3, gamble×3, toll×2, merchant×2, courier/imp/portal/shrine/spar×1) with explicit "fully conserved" tests for shrine/gamble/forge and `checkInvariants` throughout the dive suite — a dedicated "trigger-every-event" test would be redundant. Don't re-add it.
- **Hardening — loadGame quarantines unloadable saves (19y, brick 311):** the LOCAL-load mirror of 19x — `loadGame` shared the same shallow shape gate, so a parseable-but-unloadable local save wasn't quarantined → booted to "save corrupted" with NO recovery bar (bricked, worse than malformed JSON which IS recoverable). Now throws on a failed `playerView` so the existing quarantine/recovery flow (10u) catches it. 5th surface in the don't-lose-progress theme (19d/19r/19w/19x/19y). Lesson: when you fix a validation gap, grep for the same gate elsewhere the same session.
- **Hardening — import rejects unloadable saves (19x, brick 310):** `importSaveString` only checked `world` exists + `playerId` numeric — a parseable-but-broken save (bad playerId, missing agents/books) would REPLACE the current run (local + cloud) then render "save corrupted". Now gated on loadability (`playerView` succeeds; a throwing world is caught too), so a bad import is rejected before it can clobber a good run. 4th surface in the "don't lose progress" theme (19d/19r/19w/19x). **⚠ Jesse-gated product call:** should a *valid* import warn before replacing a run-with-progress (new-game 14x parity)? Import's intent is "load this", so left unguarded — paired with the 19w conflict-policy flag.
- **Data-loss fix — pristine save clobbering the cloud (19w, brick 309):** `chooseSave` was latest-wins by `lastSeenMs`, so a FRESH game on a new device (lastSeenMs=now) signing in would push its empty self OVER a real older cloud save (adopt-ELSE `pushCloudSave(local)`) → silent total progress loss. Fixed: an untouched save (`world.tick===0 && commandLog empty`) never beats a real run; recency only decides between comparable saves. Cloud sibling of 19r/19d (the "never lose progress" theme). **⚠ Jesse-gated conflict-policy call:** a *barely-played* real local (tick>0, a few commands) vs a *rich older* cloud still goes latest-wins (recency respects an intentional restart) — "prefer more progress" vs "respect restart" is a product decision; not guessed.
- **Security audit — leaderboard anti-cheat (19v, brick 308):** verdict **SOUND**. Traced client log → `verify-score` Edge Function → `verifySprint`/`replayRun` → DB. Server RECOMPUTES worth (never trusts the client), replay bounded to exactly `SPRINT_TICKS=2000` (can't buy time), startGp server-fixed, log validated, auth-gated, and **live==replay** (`newGame` `policy='idle'` matches `replayRun`; idle players run only the deterministic upgrade-gated Clerk). Two load-bearing invariants to keep true: (a) the server replays a FIXED horizon; (b) the replay agent behaves identically to the live agent. Accepted limitation: copying a verified log = strategy plagiarism (replays to its real worth), not inflation — inherent to determinism boards. Docs-only.
- **Tooling/QA — capture all three rooms (19t, brick 306):** the on-demand screenshot capture now shoots Adventure + Hall too (not just the Exchange hero), added to the README. Visual-QA pass: both rooms HEALTHY — fresh-state vertical sparseness is expected empty-state (fills with play; conditional panels correctly hidden at zero progress), not a layout bug. Closes a blind spot — ~10 Adventure/Hall features shipped this session were never rendered in QA. e2e/docs only, no redeploy.
- **Game-feel — elite boss aura (19s, brick 305):** named elites (Skarn/Vorkanth/Zukrath/Vessith) now get a pulsing gold aura in the combat scene (a `<defs>` radial gradient + `.elite-aura` ellipse, opacity-pulsed) so the climax fights look earned. Combat-only — `MonsterBody`/Bestiary glyph stays a clean silhouette. UI-only SVG/CSS, no redeploy.
- **Review fix — offline catch-up data loss (19r, brick 304):** found by the offline-subsystem adversarial review. A chunked catch-up (>5,000 ticks) interrupted by a tab-hide persisted `lastSeenMs≈now` with the world only partway → the unrun remainder (up to the 100k-tick cap, ~28h) was silently dropped on reopen. Fix: `onVis`-hidden skips the save while a chunked catch-up is in flight (`planRef.current !== null`), so the clean prior state survives and the next boot re-plans the whole gap. Also reworded the misleading "policy:'idle'" offline comment (the Clerk DOES act offline; replay re-derives it). **Logged (Jesse/cloud):** a pre-scheduled cloud push landing mid-catch-up is a rarer sibling of the same bug.
- **Feature — sell-side fill preview on the ticket (19q, brick 303):** completes 19k's buy/sell symmetry — the SELL ticket now shows "fills ≈X now · ≈Y net" (bidWalk capped at the limit) for a crossing sell, which fills at the higher resting bid (you net more than the "after tax" figure) and rests the rest. Added an optional `minPrice=0` floor to `bidWalk` (existing dump/`liquidateNow` callers byte-identical). UI-only, no redeploy.
- **Review fixes — 19e–19o adversarial pass (19p, brick 302):** independent review found two real bugs (both shipped this session). (1) reprice (19i) cancel→place could strand a BUY on `buy-limit` (allowance not refunded on cancel) — now gated on `buyRemaining ≥ remaining`. (2) away-fills (19l) counted Clerk re-quotes as fills for autoFlip players (the Clerk cancels orders in offline `runTicks`) — now suppressed (→0) when the Clerk is active. Rest of the batch verified SOUND (expectedHit/maxHit/askWalk/liquidateNow/restingQueue). **Minor edge logged (no churn):** `repriceTarget` can suggest outbidding your OWN non-best order when you hold two same-side offers on one item (rare, cosmetic — `ahead` itself is correct). **Jesse-gated:** a precise Clerk-aware away-fill count needs an engine per-player fill counter (replay-affecting → redeploy).
- **Docs/discoverability — How-to-Play refresh (19o, brick 301):** fixed the death-keep drift ("3" → "3 (5 with a Death Ward)" — the engine keeps 5 with the ward; README was already corrected 19c) and taught the 19-series instruments in the overlay (survivability/lethality reads, offer queue+reprice, true cash-out, away-fills). Anti-drift assertions pin them. **Test gotcha logged (FINDINGS #327):** loose `queryByText` negative assertions collide with the always-open HelpOverlay copy — scope to a container/class. UI-only, no redeploy.
- **⚠ Jesse-gated DESIGN question — gear-stat baseline (found 19o):** the effective-stat DISPLAYS use `deriveStats(inventory, lvls, worn)` (CharacterPanel:139, EmbarkPanel:95/150/199 — auto-uses UNEQUIPPED inventory gear, best-per-slot), while `gearDelta` compares a candidate only vs `worn` and COMBAT uses `deriveStats(exp.pack, lvls, worn)`. So three "your gear" baselines coexist (inventory / worn / pack). The gear-upgrade preview ("equips as +N") can overstate vs your current EFFECTIVE stat when you hold better unequipped inventory gear in that slot, and the embark forecast (inventory-best) can overstate vs the dive (pack). NOT a clean bug — "vs worn" is the documented intent and the embark text says "gear you pack is worn automatically" — but the three baselines are a coherence wart. Which baseline the upgrade preview/forecast SHOULD use is a balance/UX call for Jesse; behavior unchanged pending that decision.
- **Feature — in-combat lethality warning (19m, brick 299):** `maxHit(atk, def)` = `max(1, max(2,atk) − floor(def/4))` (engine damage-roll top, quest.ts:413-415). Combat view shows "⚠ a hit could down you (≤M vs H hp) — eat or flee" when the foe's worst landed hit (+ dragonfire) ≥ current hp. Worst-case complement to the mean-based forecast. Completes the survivability ladder (embark 19f → push 19g → in-fight 19m), all on the one damage model 19e verified. UI-only, no redeploy.
- **Feature — "N offers filled while away" (19l, brick 298):** the welcome-back digest now reports resting offers that fully filled during the gap. Free because of the offline idle invariant (player takes no action offline → a vanished order = a filled one): `OfflinePlan.openOrderIds0` snapshots ids pre-away, `OfflineResult.ordersFilled` = set-diff vs the book after. Closes the flipper's core away question ("did my orders go through?"). UI-only, no redeploy. NB: rides the same offline-idle assumption as the replay guard — revisit both if automation ever acts offline.
- **Feature — ask-walk buy-cost preview (19k, brick 297):** `askWalk(game, itemId, qty, maxPrice)` — buy-side mirror of bidWalk, walks `sells` UP to the limit, untaxed (buys carry no GE tax), capped at the limit price. Ticket buy preview shows "fills ≈X now · ≈C gp (avg A), Y rests" — the real immediate cost vs the qty×limit escrow ceiling. Completes the spread-walk symmetry (down for sells, up for buys). Added optional `game?` prop to TradeTicket (display read). UI-only, no redeploy.
- **Feature — honest "cash out now" on Open Positions (19j, brick 296):** `liquidateNow(game, positions)` = `{ net, units, sold }` sums each holding's `bidWalk` net (per-fill tax + own-order skip, verified 18p) → PositionsPanel header shows "cash out now ≈X" (⚠ "only S/U would fill" when bids run dry). The honest exit vs the mark-based "value" (which overstates: you can't sell N units AT last price — dumping walks down the bids). 3rd "displayed value flatters reality" find this session (cf. 19e forecast, 18w realized P&L). UI-only display read, no redeploy.
- **Feature — one-click reprice to lead (19i, brick 295):** `repriceTarget(world, order)` = the price that jumps a queued offer to the front (`sameSide[0].price ± 1` when not index 0, null when leading/off-book/sell-below-1). PlayerPanel shows a "reprice {target}" chip beside abort that fires `cancel` then `place` at the target (both logged → replay-consistent). BUY gated on re-escrow affordability so it never cancels-then-fails; SELL always safe. **Follow-up (optional):** a break-even floor on sell undercuts (needs cost basis from the TradeBook) — currently the target price shows on the button so the undercut is an informed click, matching manual-placement freedom. Repeated/auto-undercut deliberately out (one tap = one reprice).
- **Feature — queue position on resting offers (19h, brick 294):** `restingQueue(world, order)` = `{ ahead, gap }` — `ahead` is the order's index in the engine-sorted book (types.ts:92-95, so index = orders that fill before it, authoritative by construction); `gap` is the distance to the opposing best. PlayerPanel shows "· top of book" / "· N ahead" per resting offer (tooltip carries the gap + "undercut to jump the queue"). Immediate price-based diagnosis vs the lagging 500-tick stale flag (kept — complementary). UI-only display read, no redeploy.
- **Feature — live survivability read mid-dive (19g, brick 293):** carried the 19f `survivableKills` synthesis to the push-vs-extract moment — a second line in the live push-read (ExpeditionPanel) reads "≈N more kills before you'd fall · ≈M with food" at CURRENT hp, scaled by remaining packed food. Reuses the in-scope current-hp forecast `f` + `packHeal`; no new math. Sits between the push-read (odds) and death-stakes (consequence). UI-only, no redeploy.
- **Feature — survivability read on embark (19f, brick 292):** `survivableKills(forecast, hp, packHeal)` = `floor((roundsToFall/roundsToKill)·(hp+packHeal)/hp)` synthesizes the (now-accurate, 19e) forecast into "≈N kills before you'd fall · ≈M with packed food" (thin → "🍖 pack food to go deeper"). Quantifies what `embarkPrep`'s binary food warning couldn't — whether the food you packed is ENOUGH. Pure helper + one EmbarkPanel line; UI-only, no redeploy.
- **Reference-verify — `expectedHit` per-roll floor (19e, brick 291):** FIXED a 4th core-math bug from the engine-mirror seam. `expectedHit` floored the MEAN roll (`max(1,(lo+hi)/2−floor(def/4))`) but the engine floors EACH roll (quest.ts:415) → for any armoured target (def ≳ 1.3× foe atk) it under-counted incoming damage (def-60 vs fire giant: 1.0 vs engine ≈1.46; vs green dragon 1.0 vs ≈3.12, a 3× under-count) → `roundsToFall` inflated → forecast read SAFER than reality. Now averages `max(1,raw−k)` over the inclusive uniform range (rng.ts:7). Touches only the mixed-clamp regime; exact-regime values unchanged → all prior forecast tests stayed green. UI-only, no redeploy. Joins 18o/18p/18w — same lesson: when a helper "mirrors quest.ts," verify FLOOR/ROUND placement, not just the formula.
- **Adversarial review — 18g–18q (brick 278):** verdict **SOUND, zero findings** — prioritised the autonomous CORE-MATH fixes and verified each byte-identical to engine ground truth: 18o `combatForecast` folds in `hitChance` (matches quest.ts:410; engine gates each hit then adds dragonfire inside the landed-hit block, so `hitChance·(expectedHit+foeHitBonus)` is correct; no div-by-zero; `diveReadiness` stays monotonic); 18p `bidWalk.net` per-fill flooring matches `paySeller` (exchange.ts:73-74/166) + self-skip; 18g `deathRecap` keepN sort/split matches `expeditionDeath` (commands.ts:165), `view.upgrades['deathWard']` correct. 18q `f`-fights listener gates correctly (no off-tab/embark/typing fire, no collision with the embark listener). Tests confirmed to be real regression pins (accuracy test FAILS under the old formula; per-fill tax 98-vs-97; keepN 3-vs-5), not tautologies. No engine reach. **The predictor reference-verify seam is now independently verified.** Docs only, no redeploy.
- **Adversarial review — 18c–18f (brick 267):** 18c (loadout ⚠ exactly mirrors `applyLoadout`'s clamp), 18d (every added help claim verified true), 18f (hunt button never renders with a null target; mid-dive jump is a safe no-op — EmbarkPanel early-returns while diving) all **SOUND**. But 18e shipped two real defects + exposed a latent desync, all FIXED in 18g: (1) **HIGH** — the death-stakes line gated on `packGp > 0`, but carried gear/food is burned on death from the first step (`exp.pack`, before any kill mints loot gp), so a 4+ item pre-kill kit had a real stake the line hid; now gates on `recap.lostGp > 0 || recap.lostUnits > 0`. (2) **HIGH** — my 18e "hidden when no haul" test *ratified* that bug (`pack:{shark:5}, packGp:0` asserted hidden, but 2 units at risk); corrected to a true no-loss pack + added a gear-at-risk-pre-kill case. (3) **HIGH** — `deathRecap` hardcoded keep-3, ignoring the Death Ward upgrade (engine keeps `DEATH_KEEP_WARDED=5`), overstating loss in BOTH the preview and the death toast; now threads `keepN` from `view.upgrades['deathWard']` (constants imported from the engine). Verified against `commands.ts:159`. UI-only, no redeploy.
- **Adversarial review — 17w–18a (brick 262):** verdict **SOUND** across all five bricks — 17w copies-before-sort + guards empty foe-drops; 17x's shallowest-first holds (REGIONS is depth-ordered, `filter` preserves it, `||` short-circuits the elite/pool double-count); 17y's `qty−have` only renders on `!ready` (always positive) + the no-onSelect fallback is correct; 17z's `momentum()` refactor preserves the mom column's null-sink (sort + cell null exactly when `ema ≤ 0`, every `view.markets` row carries `ema`); 18a's `gapToTop` mirrors `rankGap`'s guards/tie-floor. Closed the one MEDIUM (a TEST gap, not a code defect): the 18a "redundant at #2" premise was unit-asserted but not enforced at the component gate, so a future loosening of `meRank>=3` to `>=2` would pass every test while #2 saw two identical lines — 18b adds a render test pinning `.gaptop` null + `.rankgap` present (4,000) at #2. **LOW noted (not changed):** 17z's "Noema Co" component assertion is partly redundant with the `momentum()` unit (the 0<5% threshold also excludes it), but it's valid and still guards the divide-by-zero→Infinity path. Test-only, no redeploy.
- **Adversarial review — 17q–17u (brick 256):** verdict **SOUND**. Verified the 17t goal chart line (readGoal rejects poison values; the `goal ≤ dataMax*3` fold can't push baseline/ghost/milestones off-canvas; live-read is a correctness win), the 17r compact `nth-child(5..9)` indices (match the column map — mom/margin/band/trend/swing hidden, item/bid/ask/last/volume kept), 17q `stepSpeed` (clamp + resume-from-pause asymmetry), and the 17u Escape composing with the 17s filter-Escape (App input-guard yields to the focused input). Closed the one LOW: Escape now only dismisses (+ marks seen) an OPEN help (17v gate), so a `?`-peek isn't counted as a first-run dismissal.
- **Adversarial review — 17i–17o (brick 250):** verdict **SOUND**. Verified the goal-reached toast (fires once, survives reload via persisted `goalHit`, no infinite loop, optional onToast), the deed/goal ETA math (div-by-zero guards, raw-progress passed, `paceMetric` gated), the `/`-filter + `mom` column (active-gating, null-sink, thead/tbody column-count aligned at 10), and the EmbarkPanel hoist. Fixed the one MEDIUM (clear-goal now resets `ew-goal-hit` so it's not orphaned, 17p) + documented the `paceMetric: 'worth'` ETA contract. **Cosmetic LOW noted (not changed):** `/` is swallowed while a chip/filter already has focus (the typing-guard exempts inputs/buttons before the `/` branch) — the common body-focused case works.
- **Adversarial review — session 16w–17h (brick 243):** verdict **SOUND**. Verified the per-tick hot path, the elite/duel/deed swap-guards (no 14e/14f false-fire), `awayDeeds` pre-accrual latch + reset at every `beginOffline` site, no stale closures in the once-bound MarketTable keyboard listener (navRef-fresh), the `dragon-slayer` deed fix (latched saves untouched), and the `watched`/`steady` predicates' missing-data handling. Fixed the one LOW (EmbarkPanel readiness compute now gated below the early return, 17i). **Deferred MEDIUM (not worth a fast-path absent a profile):** `refreshProgress` runs `newElites` + the alert loops + `checkMilestones` every tick; `checkMilestones`'s `cornered` deed scans all items × openOrders each tick until latched. If a profile ever shows refreshProgress hot, gate the elite/deed scans on a cheap change-detector (e.g. skip `newElites` once `eliteFired.size === ELITES.length`; memoize `cornered` per worth/inventory change).
- **Adversarial review — pure helpers (12u, brick 125):** the session's ~25 pure helpers reviewed for correctness/edge-cases — verdict **SOUND**, edges pinned as regression tests.
- **Adversarial review — effects/listeners (brick 128):** found + FIXED a HIGH bug — the sellsword autopilot shares `agent.expedition`, so its dives polluted the Delve Log (12i) + fired false player "You died" toasts. Fixed 12x (`bySellsword` snapshot gate); 12y added the render-side banner + toggle haul. Also fixed the celebration refs not resetting on restart. Remaining notes pre-existing/cosmetic (Space vs shortcut BUTTON-guard inconsistency) — non-blocking.
  - **Jesse-present follow-up:** an engine-side `bySellsword` flag on `ExpeditionState` would let the UI distinguish ownership cleanly (vs the `agent.sellsword` heuristic) and DISABLE player controls during a sellsword dive — a small replay-affecting change to batch with the verify-score redeploy.

## Deed semantics
- ~~**`dragon-slayer` fires off the producer economy, not a kill** (found 17a)~~ FIXED 17b: re-keyed from `ledger.itemsMinted['superior_dragon_bones'] > 0` (a producer mints bones within ~ticks → auto-completed with no kill) to `DRAGON_IDS.some(id => killsByMonster[id] > 0)` (`DRAGON_IDS` = the `*_dragon` monsters, self-maintaining). Safe (checkMilestones only evaluates UNlatched deeds → existing saves keep it; new games earn it correctly), UI-only, no redeploy. Regression test pins bones-but-no-kill ≠ deed. **Sweep DONE (17b recon):** audited every MILESTONE `achieved`. dragon-slayer was the ONLY deed keyed on a pure-NPC-economy artifact (`itemsMinted`, which producers advance) — now fixed. The kill deeds (Monster Slayer / Dragon Slayer / monster-scholar / apex-predator …) read `killsByMonster`/`monstersSlain`, which the SELLSWORD also advances — but that's INTENDED (the sellsword is the player's hireling, exactly like the clerk's flips counting toward the worth deeds). `cornered` reads the ledger only as the circulating-supply DENOMINATOR (correct). **One Jesse-gated residue:** `deaths` (Nine Lives) and `deepestRegion` (Pioneer/Hellwalker/Abyss-walker) flow through the SHARED `beginExpedition`/death handler (commands.ts:172,193), so a sellsword-auto dive can advance them — crediting the player with a death/region-entry a hireling did, inconsistent with the 12x "sellsword doesn't pollute personal records" principle. Fixing needs a `bySellsword` flag on `ExpeditionState` to gate the increments — a replay-affecting engine change → **batch with the verify-score redeploy + the existing bySellsword-flag follow-up** (already noted under Quality). Kill deeds intentionally stay shared.

## Trading safety
- ~~"sell the spoils" bulk button dumped GEAR too (could liquidate the raiding kit)~~ FIXED 12r (`lootSpoils` — bulk keeps gear; per-item sell still sells anything). Optional follow-up: a "sell gear too" override.
- ~~**CRITICAL (user-reported): the autoFlip CLERK sold player-bought gear before it could be equipped → satchel looked "always empty"**~~ FIXED 13d (engine: the clerk only sells items it has a cost `basis` for = its own flip-stock; won't start a flip on a player holding either). Benchmark/sim byte-identical (hash `fe75df57`). **Replay-affecting → needs the verify-score redeploy above.**

## OSRS equipment manager — ~~SHIPPED 13e~~ · Adventure-tab manager ~~SHIPPED 14h~~
**14h (Jesse-requested):** a `GearManager` on the Adventure tab — owned gear with its +stats, level requirement, and the upgrade verdict vs what's worn (↑+N / ↓-N / no gain / 🔒 train / ✓ worn), plus equip/unequip/equip-best. Reuses `gearDelta` (13h); UI-only, no redeploy. ~~sort owned gear by biggest upgrade~~ DONE 14i (usable-first, then delta desc, then name). Follow-ups: paperdoll-grid for the equipped section; sort owned gear by slot. ~~a "best affordable upgrade on the market" advisor~~ DONE 14v: `bestAffordableUpgrade(worn, lvls, gp, markets, inventory)` (pure, reuses `gearDelta`) + a "💰 best buy: {item} ⚔/🛡+N · {price} gp" line in the GearManager (affordable + usable + not-owned only; optional `view` prop). UI-only, no redeploy. ~~make it click-to-buy (jump to the Exchange + select the item)~~ DONE 14z: a "→ buy" chip on the best-buy line calls `onBuy(itemId)` (App: `setSelected` + `pickRoom('exchange')`, threaded App→ExpeditionPanel→GearManager); loads the ticket, doesn't auto-buy. UI-only, no redeploy.

**14m (Jesse-requested):** the embark "Plan a Dive" flow (region pick, danger/forecast read, prep warnings, Pack & Equip, loadouts, EMBARK) extracted from the bottom of the tall left `ExpeditionPanel` into its own `EmbarkPanel` in the Adventure tab's RIGHT column — Jesse pointed at the empty right side. Pure move (engine/commands/deriveStats byte-identical); ExpeditionPanel keeps only adventurer status + Bestiary and exports `regionDanger` for the new panel. UI-only, no redeploy. ~~a saved loadout silently clamps to held stock (apply gives you less with no signal)~~ DONE 18c: `loadoutShort(loadout, inventory)` (pure — the items where `want > have`, have=0 included, sorted; the complement of `applyLoadout`'s `min(want, have)` clamp) + a ⚠ marker + `.short` class + an "understocked: shark 1/3 …" tooltip on under-stocked loadout chips, so a reduced refill is informed instead of silent. Unit + render test. UI-only, no redeploy.


~~The user wants manually-bought gear equipped via an OSRS-style equipment manager.~~ DONE 13e: `agent.worn` per slot + `equip`/`unequip` commands (inventory↔worn, conservation-counted, level-gated) + `deriveStats(pack, lvls, worn?)` where worn OVERRIDES the pack per slot (absent worn = byte-identical old behaviour → sim hashes unchanged) + worn gear is safe on death + an Equipment section in PlayerPanel (equip chip per gear stack, unequip per slot). **Replay-affecting → in the verify-score redeploy above.** ~~an "auto-equip best" one-click~~ DONE 13f: `equipBest` command — for each slot, equips the best USABLE owned piece (satchel OR already worn; level-gated; first-seen tie-break matching `deriveStats`), displaced piece returns to the satchel, no-op when nothing improves, rejected mid-expedition + an "equip best" button in PlayerPanel. Benchmark/sim byte-identical (hashes unchanged). ~~the Adventure-tab paperdoll ignored `worn` (showed satchel-best preview, disagreed with the manager + combat)~~ FIXED 13g: `equipped(inv, lvls, worn?)` + `deriveStats(inv, lvls, agent.worn)` in CharacterPanel now reflect ACTUAL equipped gear (worn overrides the inventory pick, mirroring the engine) — figure, "in battle" effstats, and the Exchange-tab Equipped list all agree. UI-only, no redeploy. ~~the buy-upgrade decision was blind (satchel showed an equip chip with no "is this better?" signal — the user's ORIGINAL pain)~~ ADDRESSED 13h: `gearDelta(itemId, worn, lvls)` (game.ts, pure) + a per-stack badge in PlayerPanel — ⚔/🛡 `+N` green upgrade / red downgrade / 🔒 req when under-level / "no gain" sidegrade, governing stat vs what's worn. UI-only, no redeploy. ~~surface the same `gearDelta` on the buy ticket so upgrade value shows BEFORE you buy~~ DONE 13i: TradeTicket previews "equips as ⚔/🛡 ±N {stat} vs your {worn}" (+ "needs Atk N" when under-level) on a gear buy; levels threaded from App as an optional prop. UI-only, no redeploy. ~~a gear marker/filter on the MarketTable so gear is findable among the 128 commodities~~ DONE 13j: a ⚔/🛡 marker on every gear row (titled with slot + level req) + a "gear" track filter isolating the equippable subset; reuses the engine `GEAR` map as the taxonomy. UI-only, no redeploy. Remaining follow-ups: a richer paperdoll GRID (per-item silhouettes in slot layout) vs the current SVG figure + list; the "gear" track could sort by upgrade delta vs what's worn (best buy first); decide if deep-region death should risk worn gear (currently always safe).

## Old minor leftovers (non-blocking)
- Trades window: `shift()` → ring buffer if it ever grows beyond 512.
- Momentum traders bleed slowly (FINDINGS #12) — acceptable.
